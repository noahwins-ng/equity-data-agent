# Equity Data Agent — Project Plan

Progress tracking for the phased build-out. Each item maps to one or more Linear issues.
Updated automatically by `/ship` and `/sync-docs`.

---

### Phase 0 — Foundation
**Scope**: Repo scaffolding, infrastructure, and CI/CD.

- [x] Initialize monorepo with uv workspaces (4 packages) — QNT-34
- [x] Set up root `pyproject.toml` with shared dev dependencies (ruff, pyright, pytest)
- [x] Create `shared` package with `Settings`, ticker registry (`TICKERS` list + `TICKER_METADATA` dict with sector/industry per ticker), and base Pydantic schemas — QNT-35
- [x] Write `docker-compose.yml` with dev/prod profiles — QNT-36
- [x] Write `Dockerfile` (multi-stage: base with uv deps → dagster target, api target) — shared by dagster, dagster-daemon, and api services
- [x] Set up ClickHouse with DDL migration scripts (raw + derived databases) — QNT-37
- [x] Configure GitHub Actions for CI (lint + test) and CD (SSH deploy) — QNT-38
- [x] Create `.env.example` with all required environment variables — QNT-39
- [x] Bootstrap Hetzner CX41 production server: provision VPS, install Docker, configure GitHub deploy secrets, first manual deploy — QNT-83
- [x] Integration test infrastructure + prod health visibility — QNT-85
    - `/health` endpoint with ClickHouse connectivity check (200 ok / 503 degraded)
    - `tests/integration/` with auto-skip when ClickHouse unreachable locally
    - CI: ClickHouse service container + integration test step on every PR
    - Deploy pipeline: post-deploy health check gate (fails if API doesn't come up within 60s)
    - `make check-prod` and `make test-integration` helpers
- [x] Claude Code slash command framework (12 commands in `.claude/commands/`) and dev workflow docs (`docs/guides/dev-workflow.md`, this `project-plan.md`) — QNT-84
- [x] Verify: SSH tunnel to ClickHouse works, Dagster UI starts locally, CI pipeline passes

---

### Ops & Reliability
**Scope**: Cross-phase hardening that responds to prod incidents. Each item is reactive — triggered by a specific failure mode rather than a planned Phase deliverable. Lives outside the Phase 0–7 axis because the work cuts across phases.

- [x] CD hard gate: verify prod `git rev-parse HEAD` equals merged commit SHA — QNT-88
    - **Triggered by**: Apr 16 2026 outage during Phase 2 Calculation Layer work — CD reported success while prod was 17 commits behind main. Root cause was a SCP'd hotfix that blocked `git pull` during deploy, masked by a passing /health check on stale code.
- [x] CD hard gate: verify Dagster definitions module loads expected asset / check / schedule counts — QNT-89
    - **Triggered by**: Same Apr 16 2026 outage — container uptime didn't prove the deployed Python actually loaded the asset graph we thought we shipped.
- [x] Harden /go pipeline with three-class AC taxonomy (code vs dev-exec vs prod-exec) — QNT-90
    - **Triggered by**: Apr 16 2026 retrospective during Phase 3 API Layer work — `/sanity-check` had been marking AC ✓ based on code inspection alone, so "shipped but broken in prod" was technically allowed. Introduced the three-class AC taxonomy + evidence requirements + `/ship` post-deploy hard gates.
- [x] Add `restart: unless-stopped` to prod services in docker-compose.yml — QNT-95
    - **Triggered by**: Apr 18 2026 outage immediately after shipping QNT-51 (Phase 3 `/health` endpoint) — Hetzner VPS rebooted for a kernel update at 04:00 UTC, all 6 containers cleanly exited with `Exited (0)`, nothing came back up. ~48 min API outage until manual `docker compose --profile prod up -d`. Docker default restart policy is `no`.
- [x] Alert on pending kernel reboots (health-monitor log + unattended-upgrades mail via Resend SMTP) — QNT-96
    - **Triggered by**: Same Apr 18 2026 outage — `/var/run/reboot-required` had been set 21 hours earlier by `unattended-upgrades`, but no-one saw it. Fix adds a `REBOOT REQUIRED` line to `scripts/health-monitor.sh` (surfaced by `make monitor-log` + session-start hook) and wires `Unattended-Upgrade::Mail` through a postfix → Resend SMTP relay (documented in `docs/guides/hetzner-bootstrap.md` §10).
- [x] Create ops runbook skeleton with failure-mode catalog — QNT-99
    - **Triggered by**: Apr 19 2026 retro — the Ops & Reliability work has turned specific incidents into permanent detectors, but there's no consolidated document to grep when something breaks at 3am. Runbook is the index into the muscle memory. Small scaffolding ticket; subsequent Ops & Reliability tickets add their own entries.
- [x] Harden docker-compose.yml: HEALTHCHECK + log rotation + resource limits — QNT-100
    - **Triggered by**: Apr 19 2026 retro — raw compose defaults leave three specific gaps we hadn't closed: "sick but still up" (no healthchecks), "disk fills with logs" (no rotation), "one leaky service OOMs the box" (no resource limits). Addressing each directly on the existing stack.
- [x] Alerting pipeline: uptime monitoring + container state notifications — QNT-101
    - **Triggered by**: Same Apr 19 2026 retro — Apr 18 outage surfaced that `/health` failures go into a log file nobody reads. Need real pager (SMS/email) for downtime + Discord notifications for container state changes.
- [x] Autoheal sidecar for unhealthy long-running containers — QNT-104
    - **Triggered by**: QNT-100 deferrals — plain compose's `restart: unless-stopped` only restarts on container *exit*, not *unhealthy* status; sick-but-still-up is a known gap on the long-running services (`api`, `dagster-code-server`, `dagster-daemon`, `dagster`, `litellm`). `willfarrell/autoheal` sidecar polls Docker health status and kills containers labelled `autoheal=true` when they go unhealthy, so `restart: unless-stopped` picks them up. Run workers are already covered by QNT-116's DockerRunLauncher (`supports_check_run_worker_health=True` → ~2 min STARTED-orphan recovery via `monitor_started_run`); this ticket closes the parallel gap on the long-running services.
    - **Rescoped 2026-04-27**: original ticket also bundled "tighten `mem_limit` / `cpus` to peak + ~30% headroom after 1-2 weeks of QNT-100 sizing observation". Dropped — QNT-116's topology shift made the QNT-100 reference numbers (daemon 3g, webserver 2g) obsolete, and run workers now live in their own per-run cgroups with no shared budget to right-size. If a fresh mem_limit audit is needed later, file as a separate ticket against post-QNT-116 observation.
- [x] Raise dagster-daemon mem_limit 1g → 2g after Apr 20 weekly-fundamentals OOM cascade — QNT-111
    - **Triggered by**: Apr 20 02:00 UTC weekly schedule tick — `fundamentals_weekly_schedule` fired 10 partitions, the daemon's 1g cgroup (set by QNT-100) was blown out by concurrent partition-launcher subprocesses. Kernel OOM-killed 4+ python processes in ~2 min (one hit total-vm 2GB), 7 partitions failed to launch, 2 runs orphaned in CANCELING for ~10.5h. Narrow stopgap so next Sunday's tick doesn't cascade; the comprehensive observed-peak audit was rendered moot by QNT-116's topology change (daemon 3g → 512m, run workers in their own cgroups). QNT-104 retains the autoheal-sidecar half only.
- [x] Two-layer deploy-window retry protection for sensor + schedule jobs — QNT-110
    - **Triggered by**: Apr 19 same-day shipping cadence — 2 `ohlcv_downstream_job` runs failed with `DagsterUserCodeUnreachableError: gRPC UNAVAILABLE` when dequeued mid-deploy (code-server container was restarting). No op ran, so the ticket's originally-proposed op-level `RetryPolicy` wouldn't have caught it. Shipped **both layers**: op-level `DEPLOY_WINDOW_RETRY` on sensor jobs (catches in-run flakes like yfinance timeouts) + run-level `run_retries` in `dagster.yaml` with per-job `dagster/max_retries` tags on all 4 auto-triggered jobs (catches launch-time failures). `retry_on_asset_or_op_failure: false` ensures real op errors still fail loud — only launch failures retry. Complements QNT-109 (notification suppression during the same deploy window).
- [x] Cap Dagster concurrent runs at 3 with QueuedRunCoordinator to prevent backfill fan-out OOM — QNT-113
    - **Triggered by**: Apr 20 13:22–13:28 UTC manual backfill — 10-partition `fundamentals_weekly_job` launched all 10 subprocess workers simultaneously (DefaultRunCoordinator fires immediately, no queue). Each worker loads the full repo (~150 MB RSS / ~2 GB VM); combined load re-OOM'd the daemon's 2 GB cgroup even after QNT-111's bump. QNT-110 retries re-launched into the same starved cgroup, looping backfill `tevuzzoj` into a 10:31 failure (partition AMZN stuck at "Failed to start"). Fix adds `QueuedRunCoordinator(max_concurrent_runs=3)` to `dagster.yaml` — peak memory now ~1.1 GB within the 2 GB limit (daemon baseline ~260 MB + sensor-tick headroom ~400 MB + 3 × 150 MB). QNT-111 addressed the daemon's baseline; QNT-113 addresses the fan-out. The broader resource-limit audit originally tracked on QNT-104 was superseded by QNT-116's per-run cgroup topology.
- [x] Raise dagster-daemon mem_limit 2g → 3g after QNT-113 sizing math under-estimated per-worker peak — QNT-115
    - **Triggered by**: Apr 21 2026 12:22 / 12:48 UTC — the same `__ASSET_JOB` backfill (MSFT/GOOGL/AMZN) that motivated QNT-114 also pushed the daemon cgroup to 1.74 GiB / 2 GiB (87%) and the kernel OOM-killed python run-worker subprocesses. QNT-113 had sized the cap assuming 150 MB RSS per worker (reasonable for the Apr 20 `fundamentals_weekly_job` which mostly did repo-load), but observed peak during `__ASSET_JOB` *materialization* is ~360 MB, so real load was 660 + 3 × 360 = 1740 MB — right at the limit with no headroom. Fix: bump `docker-compose.yml` dagster-daemon `mem_limit` 2g → 3g; `max_concurrent_runs` stays at 3 (this is headroom, not more parallelism). Also revised the "Adding a Dagster Asset → Concurrency pre-flight" formula in `docs/patterns.md` from `/150 MB` to `/360 MB` and swept four stale call sites (`dagster.yaml`, `ops-runbook.md`, `schedules.py`, `ops-investigator` symptom card) so the sizing math is consistent everywhere. Theoretical ceiling at 3g is ~6; practical cap stays at 3. Pairs with QNT-114: run_monitoring auto-heals ghosts when OOMs still happen, mem bump reduces the rate at which OOMs happen in the first place.
- [x] Enable run_monitoring + backfill tag concurrency in dagster.yaml — QNT-114
    - **Triggered by**: Apr 21 2026 post-incident backfill — `__ASSET_JOB` (MSFT/GOOGL/AMZN partitions) OOM'd the daemon cgroup; the AMZN run-worker was kernel-killed before emitting `RUN_FAILURE`. Operator "Terminate" flipped it from STARTED → CANCELING; the run never progressed because CANCELING requires the worker to ack and the worker was dead. The ghost slot held 1 of 3 `max_concurrent_runs`, the backfill daemon kept auto-relaunching AMZN producing more ghosts, and the queue wedged ~30 min until the daemon was restarted. Root cause: Dagster ships with `run_monitoring` disabled by default; no daemon was polling the CANCELING rows. Shipped in two PRs plus a post-ship doc clarification: (1) PR #94 enabled `run_monitoring` + `tag_concurrency_limits: dagster/backfill: 2`; (2) post-ship live chaos test on prod (injected a STARTED row) surfaced that `DefaultRunLauncher` returns `supports_check_run_worker_health = False`, so `monitor_started_run`'s worker-health branch is a no-op for us; (3) hotfix PR #95 added `max_runtime_seconds: 1800` as a timeout fallback so STARTED ghosts with no operator Terminate also get recovered (after 30 min). Important correction captured in the runbook: the Apr 21 incident was actually the **CANCELING class**, recovered by `monitor_canceling_run` within `cancel_timeout_seconds` (~3 min) — that path is launcher-independent and was already covered by PR #94. The hotfix closes a separate STARTED-no-Terminate class the incident didn't hit. Three orphan classes total, three code paths, three timeouts — documented as a table in `ops-runbook.md` under "CANCELING ghost after run-worker OOM". A proper launcher switch to `DockerRunLauncher` (which advertises health-check support → STARTED recovery in ~2 min instead of 30 min) is tracked as a separate follow-up ticket. Complements QNT-113 (fan-out cap) and QNT-110 (launch-time retry). Durable lesson captured: "run_monitoring.enabled: true" is necessary but not sufficient — each orphan class (STARTING / CANCELING / STARTED) uses a different code path with different launcher dependencies; validate which class your incident was before declaring recovery proven.
- [x] Bind-mount dagster.yaml so repo edits actually reach the running daemon — QNT-112
    - **Triggered by**: Apr 20 QNT-110 ship session discovery — `dagster_home` named volume held a stale copy of `dagster.yaml` seeded on Apr 16 initial deploy; `git pull` on every deploy updated the host file but never the container. QNT-110's `run_retries` config silently failed to activate despite CD green + all hard gates passing. Same class as Apr 16 SCP-drift and Apr 18 "deploy green ≠ code active" — aggregate green signals hid an invariant. Fix: bind-mount `./dagster.yaml:/dagster_home/dagster.yaml:ro` on both `dagster` and `dagster-daemon`. Repo is now the single source of truth for dagster.yaml; named volume keeps Dagster-managed state (history/storage/schedules) intact. PR also includes a smoke-marker comment in `dagster.yaml` so the deploy self-verifies the delivery path end-to-end.
- [x] Docs: swap uptime monitoring guide from BetterStack to UptimeRobot — QNT-105
    - **Triggered by**: QNT-101 rollout — BetterStack's free tier requires a paid plan for Discord webhooks, while UptimeRobot ships native Discord integration on free. Zero-cost polish to match the free-tier Discord path the project already uses elsewhere.
- [x] API accepts HEAD on /health endpoints for HEAD-only uptime probes — QNT-106
    - **Triggered by**: QNT-105 uptime-monitoring switch — UptimeRobot defaults to HEAD probes; FastAPI's auto-generated GET routes return 405 on HEAD. Explicit HEAD handlers let the uptime probe pass cleanly without needing UptimeRobot's paid advanced-monitor tier.
- [x] Polish docker-events-notify: fix `<no value>` exit-code display + correct restart-policy docs — QNT-107
    - **Triggered by**: QNT-101 runtime testing — Docker kill events don't carry `exitCode`, so Go template `<no value>` sentinel leaked past the bash `${var:-default}` fallback. Also corrected Makefile + uptime-monitoring.md that implied `restart: unless-stopped` auto-restarts after `docker kill` (it doesn't — Docker treats both `stop` and `kill` as "manually stopped" and skips the restart policy).
- [x] Suppress docker-events alerts during CD deploy window — QNT-109
    - **Triggered by**: QNT-101 noise during deploys — every main-branch merge fires ~4 Discord messages ([KILL]+[DIE] on api from GIT_SHA-driven recreate, [DIE] on both dagster services from image rebuild) plus 2-3 "can not get logs" races. Expected churn, not real incidents — but trains users to ignore the channel, eroding QNT-101's pager purpose. Sentinel-file deploy window (`/opt/equity-data-agent/.deploy-in-progress`) suppresses notifier events during CD; fail-open (>10 min) so a crashed deploy can never silently mute real alerts.
- [x] Encrypt .env at rest with SOPS — QNT-102
    - **Triggered by**: Same Apr 19 2026 retro — plaintext `.env` on VPS = all credentials leak on compromise. Replace with SOPS-encrypted file + decrypt-on-deploy. (ClickHouse backup ticket deferred: current data <1GB, re-ingestible from yfinance in 1-2h; revisit after Phase 4 news+embeddings populate.)
- [x] Widen fundamental_summary P/E asset-check band to symmetric [-10000, 10000] — QNT-119
    - **Triggered by**: Apr 23 2026 — `fundamental_summary_pe_in_band` WARN on AMZN 2022-12-31 annual (P/E = -1008.89, EPS = -0.253 × latest close $255.36). Value is arithmetically correct — today's price × AMZN's 2022 Rivian-writedown loss year — not corruption. Root cause: the check band (-1000, 10000) was asymmetric but the N/M floor `_EPS_NM_THRESHOLD = 0.10` in `fundamental_summary.py` was calibrated to cap |P/E| at ~10k on both signs, so the tight -1000 lower bound flagged legitimate negative-earnings years the N/M floor already intentionally let through. Fix: widen `_PE_MIN` to -10_000.0 for symmetry, update docstring + inline comment so the two calibrations don't drift silently. True arithmetic corruption still trips at |P/E| > 10k. Reinforces `feedback_asset_checks_catch_real_bugs.md` — the asset check surfaced a real calibration inconsistency (not a data bug), which is a valid catch.
- [x] Resource hygiene: narrow retry-on-Exception in ClickHouse + Qdrant resources — QNT-117
    - **Triggered by**: QNT-54 code review (2026-04-22) — `ClickHouseResource` and `QdrantResource` both retry on bare `except Exception`, burning the 3×2s budget on non-transient errors (wrong API key, 4xx, schema mismatch) that should fail loud immediately. Scope: retry only on transport/timeout/5xx — `clickhouse_connect.driver.exceptions.OperationalError` for ClickHouse, `httpx.TransportError` + `qdrant_client.http.exceptions.UnexpectedResponse` 5xx for Qdrant; everything else re-raises. **Rescoped 2026-04-26**: the original ticket also bundled client-reuse / `setup_for_execution` caching, dropped because QNT-116's DockerRunLauncher topology means each run is its own short-lived container — the resource instance only lives for one run-worker, so per-call client construction is bounded and not load-bearing. Revisit only if profiling shows it matters.
- [x] Audit asset checks for composite-key aggregation correctness — QNT-122
    - **Triggered by**: Phase 4 retro (2026-04-23) — QNT-120 surfaced that QNT-93's `news_embeddings_vector_count_matches_source` compared Qdrant per-ticker point count against `count() FROM news_raw`, but the asset keys Qdrant on `(ticker, url_id)` so the correct aggregation was `uniqExact(id)`. The check was silently off-by-dedup for a week until QNT-120 exposed it (9/10 tickers showed false drift from re-published articles). `feedback_fix_pattern_not_example.md` says sweep for every instance; this ticket is that sweep across indicator / fundamental / news asset checks. Output: audit note per check in `docs/retros/phase-4-asset-check-audit.md`, follow-up fix tickets for any mismatches found.
- [x] Migrate Dagster to production topology: code-server split + DockerRunLauncher — QNT-116
    - **Triggered by**: Apr 21 2026 21:13–21:26 UTC gRPC-UNAVAILABLE cascade (QNT-115 window) — code-loading subprocess OOM'd inside the `dagster-daemon` cgroup at the 3g ceiling (had just been raised 2g → 3g hours earlier), the code server was unavailable for 180s, and 8 `ohlcv_downstream_job` runs transitioned to FAILURE without launching a step, 5 in lockstep at 21:26 as submission-path retries timed out simultaneously. The incident hit the ceiling of the QNT-100/111/113/115 `mem_limit`-bump ratchet: 3g was already saturated under fan-out so the next bump wasn't going to hold. Root cause: the daemon container was doing jobs Dagster's production deployment docs explicitly carve out into separate services. Shipped Dagster's canonical Docker Compose topology in one PR: (1) split user code into `dagster-code-server` (own `mem_limit: 2g`, gRPC on :4000, healthcheck via `dagster api grpc-health-check`); webserver and daemon reach it via `workspace.yaml`; (2) replaced `DefaultRunLauncher` with `DockerRunLauncher` so each run is an ephemeral container with its own cgroup (per-run OOM no longer touches siblings). Daemon `mem_limit: 3g → 512m`, webserver `2g → 1g`. The DockerRunLauncher also flips `supports_check_run_worker_health = True`, closing QNT-114's chaos-test finding that `monitor_started_run`'s health branch was a no-op on `DefaultRunLauncher`: STARTED-orphan recovery drops from ~30 min (`max_runtime_seconds` fallback) to ~2 min (verified in dev smoke: `docker kill` → FAILURE in 30s via Docker-API `ExitCode: 137` detection — 60× improvement). Three first-boot issues caught + fixed during dev smoke that would have broken prod: workspace.yaml bind-mount path conflict with shared named volume, concurrent-init alembic race (fixed via `depends_on: service_healthy`), run-worker containers couldn't reach SQLite run storage (fixed via `container_kwargs.volumes`). Compose project name pinned to `equity-data-agent` at top-level so DockerRunLauncher network/volume references (which hardcode the prefix) fail loudly at parse time rather than silently at run-launch time if anyone renames the repo dir. ADR-010 captures the decision + alternatives + revisit triggers (SQLite `database is locked`, outgrowing single-VPS, second code-location). Retires the `mem_limit`-bump cycle; QNT-118 (lazy-import sweep) compounds this work but ships independently.
- [ ] Lazy-import heavy deps in asset modules to shrink per-subprocess RSS — QNT-118 **[deferred 2026-05-06; revisit-when triggers in ticket body]**
    - **Triggered by**: QNT-116 follow-up (2026-04-22) — every Dagster subprocess (code server, run workers, sensor/schedule evaluators) pays top-level `import pandas / numpy / qdrant-client / yfinance / clickhouse-connect` at startup, even when the subprocess never touches the library. QNT-115's revised per-subprocess peak was ~360 MB; hypothesis is the majority is heavy library imports, and deferring them into function bodies should drop per-subprocess RSS to ~120–150 MB. Scope: `from __future__ import annotations` + function-local imports across every module in `packages/dagster-pipelines/src/dagster_pipelines/`; `TYPE_CHECKING` guards for any Pydantic resource fields that annotate heavy external types. Compounds QNT-116's topology gains (more subprocesses to amortize across) but ships independently so rollback is orthogonal. Measurement AC compares pre/post `docker stats` on `dagster-code-server` under a sensor-tick storm.
    - **Deferred 2026-05-06 (Phase 7 close review)**: post-QNT-116, dagster-code-server steady-state RSS is 351 MB inside a 2 GB cgroup (17% util) and run-workers live in their own ephemeral cgroups — the shared-budget ratchet that motivated this work is gone, so the measurement AC has no current load to validate against. Revisit-when triggers in the Linear body: code-server peak >80% of 2 GB, OR a new asset module pushes peak past 1.5 GB, OR `Backoff.EXPONENTIAL` retries fail because of fresh-subprocess startup latency.
- [x] CD: restart services whose bind-mounted config changed — QNT-124
    - **Triggered by**: QNT-123 post-deploy verification (2026-04-23) — first POST to `equity-agent/gemini` after the Pro → Flash config change still routed to Pro and returned HTTP 429. Prod SHA matched the merge commit, hard gates green, but `docker compose ps` showed `litellm` Up 21 min — predating the deploy. Root cause: `docker compose up -d` only recreates a container when the *compose service definition* changes (image, command, env_file, environment), NOT when a bind-mounted file changes on disk; `litellm_config.yaml` is bind-mounted and read once at startup with no hot-reload, so the file on disk was new but the running process was stale. Same class as the Apr 16 SHA-drift outage and QNT-112 named-volume-shadow — aggregate-green signals hiding a runtime invariant. Fix: post-`docker compose up -d` step in `.github/workflows/deploy.yml` detects changed bind-mounted config files (`litellm_config.yaml`, `dagster.yaml`, `workspace.yaml`, `Caddyfile`) in the merged diff and `docker compose restart`s affected services before QNT-89's Dagster-graph hard gate runs, so the gate exercises the reloaded config.
- [x] Guard dagster.yaml env_vars against Settings drift — QNT-125
    - **Triggered by**: 2026-04-24 outage — 200/200 Dagster runs failed at dequeue with `Tried to load environment variable OLLAMA_API_KEY, but it was not set` for ~20h before user detection ("why so many failures in Dagster wtf"). Root cause: QNT-59 (Phase 5 LiteLLM swap, PR #110) removed `OLLAMA_API_KEY` + `ANTHROPIC_API_KEY` from `shared.config.Settings` and `.env` but left both keys in `dagster.yaml::run_launcher.config.env_vars`. `DockerRunLauncher` treats the env_vars list as a passthrough contract and drops every run at dequeue when a listed key isn't in the daemon env — the daemon container stays healthy (so uptime probe + docker-events-notify see nothing), `/health` keeps returning 200, and none of the three alerting channels fired. Same "aggregate-green signals hiding a runtime invariant" class as Apr 16 SHA drift, QNT-112 named-volume shadow, and QNT-124 stale bind-mounted config — but on an orthogonal axis: QNT-124 catches "running process vs file on disk"; QNT-125 catches "file on disk vs Settings shape". The previous guard was an inline `# audited as of 2026-04-22` comment, which drifted silently on the very next PR after it was written; this ticket replaces it with a CI test (`tests/dagster/test_run_launcher_env_vars.py`) that asserts `set(env_vars) ⊆ set(Settings.model_fields) ∪ {DAGSTER_HOME}` on every push and fails loud on re-drift (verified via inject-and-restore: bogus key → AssertionError with remediation text, restore → green). Queue-drop alerting (the sibling gap that let this run ~20h undetected) is scoped separately in QNT-62. Calibration note: the Phase-5 LLM swap PR should have been the trigger to run the Settings-diff audit, not a comment promising it was already done — the memory rule "comments are not contracts" is now enforced by CI.
- [x] Migrate orphaned `packages/*/tests/` into `tests/` so pytest actually runs them — QNT-127
    - **Triggered by**: 2026-04-24 QNT-61 adversarial review — a new agent test file landed in `packages/agent/tests/` and was never collected. Root cause: `pyproject.toml` pins `[tool.pytest.ini_options] testpaths = ["tests"]`, so pytest only walks the repo-root `tests/` tree; any `test_*.py` under `packages/<pkg>/tests/` is silently skipped by `uv run pytest` and CI. The drift was months old — ~1,340 lines of API tests + one dagster test had never executed in CI, including `tests/api/test_health.py`'s QNT-51 `/health` deploy-identity contract (a regression there would have shipped uncaught). Fix: (1) `git mv` 7 files into `tests/api/` + `tests/dagster/` — collection 76 → 152 tests, full suite green on first run (no bit-rot to repair); (2) add `tests/test_no_orphan_tests.py`, a CI guard symmetric to QNT-125's env_vars guard that fails loud if any `test_*.py` / `*_test.py` reappears under `packages/*/tests/**` at any depth, with a remediation hint pointing at this ticket. Same durable pattern as QNT-125: replace reviewer vigilance with a machine-enforced contract. Post-review the glob was deepened from shallow `*/tests/test_*.py` to cover nested subdirs + pytest's alternate `*_test.py` discovery convention; both variants negative-tested.
- [ ] Rotate GROQ_API_KEY + GEMINI_API_KEY leaked in 2026-04-24 session transcript — QNT-126
    - **Triggered by**: 2026-04-24 session transcript review — both API keys echoed in plaintext into a local Claude Code session transcript during the QNT-125 outage debug. Transcripts persist on disk under `~/.claude/projects/` and aren't encrypted at rest. Rotation issues new keys at Groq + Google AI Studio, updates SOPS-encrypted `.env` on prod (per QNT-102), redeploys LiteLLM, and verifies the proxy routes cleanly under both `equity-agent/default` and `equity-agent/gemini` post-rotation.
- [x] Security: scanner suite in CI (pip-audit + npm audit + bandit + gitleaks + trivy weekly) — QNT-160
    - **Triggered by**: 2026-05-02 QNT-75 Vercel deploy made the public Hetzner API a real attack surface; CI runs lint + types + tests but does no CVE scanning, secret detection, or static-analysis. Lands a layered scanner suite as a PR gate (pip-audit / npm audit / bandit / gitleaks blocking on high+critical) plus a weekly trivy image-scan cron. Pairs with `.github/dependabot.yml` for grouped weekly bumps + immediate security PRs. README badges + posture paragraph make the supply-chain hygiene visible to portfolio readers without forcing a YAML dive.
    - **Triggered by**: 2026-04-24 session transcript review — both API keys were echoed in plaintext into a local Claude Code session transcript during the QNT-125 outage debug. Transcripts persist on disk under `~/.claude/projects/` and are not encrypted-at-rest. Rotation issues new keys at Groq + Google AI Studio, updates SOPS-encrypted `.env` on prod (per QNT-102), redeploys LiteLLM, and verifies the proxy routes cleanly under both `equity-agent/default` and `equity-agent/gemini` post-rotation.
- [x] Auto-apply ClickHouse migrations on every deploy — QNT-146
    - **Triggered by**: 2026-04-29 QNT-134 (PR #152) — code shipped expecting new columns (`sma_200`, `adx_14`, ..., `revenue_ttm`) but migrations 015-023 had been committed to the repo without ever being applied to prod. Dagster materialization failed with `ProgrammingError: Unrecognized column 'sma_200'`; recovery took 60+ min of manual `ssh hetzner 'curl localhost:8123 ...'` per migration, then a hotfix PR (#154). Same "aggregate-green signals hide a runtime invariant" class as Apr 16 SHA-drift, QNT-112 named-volume shadow, QNT-124 stale bind-mount, QNT-125 env_vars drift — but on the schema axis. The `make migrate` step had been described as part of CD in three docs (README, system-overview, project-requirement) for months, but `deploy.yml` never actually ran it; the docs were aspirational. Fix: inline curl loop in `deploy.yml` between `Deploy to Hetzner` and `Restart services` that POSTs every `migrations/*.sql` to `localhost:8123` with `set -euo pipefail` + curl `--fail` so a single bad migration aborts CD. All 23 migration files use idempotent DDL (`CREATE … IF NOT EXISTS`, `ADD COLUMN … IF NOT EXISTS`) so re-applying on every deploy is a no-op when nothing changed. Also adds a 90s `/ping` readiness probe in case a future ClickHouse image bump restarts the container, and `shopt -s nullglob` so an empty migrations dir is a clean skip rather than a literal-glob failure. Docs swept to remove the `make migrate` aspiration from CD-flow descriptions; bootstrap docs intentionally retain it as a fresh-DB local tool. Pairs with the structural lesson from `feedback_reactive_sizing_trap` — running migrations manually each ticket was the parametric fix; this is the structural one.
- [x] Vercel ISR Writes overrun: 60s revalidate vs EOD data cadence — QNT-166
    - **Triggered by**: 2026-05-03 — Vercel project metrics showed ISR Writes climbing on the 60s `revalidate` baked into Phase 6 ticker pages, while the underlying ClickHouse-derived data refreshes once per day at most. Two-part fix: (1) bump ISR `revalidate` 60s → 3600s → 86400s so a single page render serves an entire trading day; (2) add `vercel.json` `ignoreCommand` so backend-only commits don't trigger a frontend rebuild on Vercel (CD path is `git push` → Vercel webhook, so backend PRs were burning ISR Write budget for no UX delta). Same "infra cost climbs silently because no alert is wired to the metric" class as the QNT-167 disk-reclaim finding — observability surface added in this cycle (Vercel project metrics) caught it before it became a billing event.
- [x] Disk reclaim + DockerRunLauncher auto_remove (1080 stopped containers, 53 GB build cache, /dev/sda1 at 85%) — QNT-167
    - **Triggered by**: 2026-05-03 — `HostDiskHigh` Grafana alert fired after QNT-103's node_exporter mountpoint fix (PR #200). `docker system df` showed 1080 stopped Dagster run-worker containers from QNT-116's DockerRunLauncher (52 GB) and 53 GB of stale build cache from `--build`-on-every-CD; /dev/sda1 was 85% full. Two-part fix: (1) one-time `docker container/builder/image prune` on prod (85% → 17%, +94 GB freed; running services and named volumes untouched); (2) structural prevention via `auto_remove: true` in `DockerRunLauncher.container_kwargs` so future run-workers self-delete (race with `run_monitoring`'s `check_run_worker_health` is closed by Dagster's existing run-state re-fetch guard at `monitor_started_run` line 125 — verified against `dagster/_daemon/monitoring/run_monitoring.py`); (3) ongoing build-cache hygiene via `make prune-build-cache-install` cron (Sun 04:00 UTC weekly, `--filter unused-for=168h`). Trade-off captured in the dagster.yaml comment: container-level forensic state (`State.OOMKilled` flag, `docker inspect` output) is lost on auto-remove, but Dagster's SQLite event log on the `dagster_home` volume preserves run logs, QNT-101 docker-events-notify still emits OOM events to Discord, and cAdvisor (QNT-103) retains per-container memory traces — acceptable to avoid 1000+ Exited containers on a 150 GB host. Same "observability did its job" pattern as QNT-100 → QNT-116 arc: HostDiskHigh fired before disk pressure became an outage. Runbook entry "Disk usage climbing on prod" added to `ops-runbook.md`.
- [x] Switch frontend from ISR to SSG + Vercel Deploy Hook — QNT-168
    - **Triggered by**: 2026-05-04 — third consecutive ticket ratcheting the same dial on the same surface (QNT-166: 60s → 3600s → 86400s; QNT-167: vercel.json `ignoreCommand`; QNT-168 v1: on-demand `revalidateTag` + token + Dagster sensor). Mid-implementation the reactive-sizing trap was named: ISR is for unknown-cadence content, but our data mutates exactly once per Dagster ingest cycle in known windows by a system we control. Pivoted in-place from "smarter ISR" to "no ISR." `/ticker/[symbol]` and `/` are now `dynamic = "force-static"` + `dynamicParams = false`; `apiFetch()` default switched from `next: { revalidate }` to `cache: "force-cache"` so build-time fetches pin into the static output; status indicators (provenance strip, watchlist next-ingest label) became client components with `cache: "no-store"` (the only piece worth keeping from QNT-168 v1). Three new Dagster schedules in `dagster_pipelines.vercel_deploy` POST a Vercel Deploy Hook URL ~35 min after each ingest cron (ohlcv weekday 17:35 ET, news daily 02:35 ET, fundamentals Sun 22:35 ET); the hook URL itself is the auth — no shared secret across two control planes. Failure to POST logs a warning, never fails the run; prior deploy keeps serving on a missed deploy. Net effect: zero ISR Writes by definition, drops `/api/revalidate` route handler + `REVALIDATE_SECRET`/`FRONTEND_BASE_URL` env coupling + per-ticker tag plumbing. ADR-014 §1 + §3 superseded with a Revisions block citing the calibration story. Same `feedback_reactive_sizing_trap` lesson as QNT-100 → QNT-116 (Dagster topology) and QNT-146 (ClickHouse migrations on every deploy): when N consecutive tickets raise the same parametric bound, the next ticket is always structural. Pre-deploy ops handoff: human generates the Vercel Deploy Hook (Vercel → Settings → Git → Deploy Hooks) and `sops set`s it into `.env.sops` post-merge; until then the op skips with INFO log — safe-default behavior.
- [x] Forward VERCEL_DEPLOY_HOOK_URL to Dagster run-worker containers — QNT-171
    - **Triggered by**: 2026-05-05 — frontend served May 1 data for ~5 days; ClickHouse had OHLCV through May 4 + news through May 5. Dagster `vercel_deploy_after_*` schedules (QNT-168) were ticking SUCCESS but the op silently logged `URL not set; skipping deploy trigger` because `VERCEL_DEPLOY_HOOK_URL` was added to `Settings` + `.env.sops` but never to `dagster.yaml`'s `run_launcher.config.env_vars` allowlist — DockerRunLauncher drops anything not on that list when spawning the per-run container. The lone successful Dagster-hook deploy (May 4 12:50 UTC) was a manual `docker exec dagster-daemon dagster job execute` run that inherits `env_file: .env` directly and bypasses DockerRunLauncher (visible in the run tags: no `dagster/schedule_name`, no `docker/container_id`). Same shape as QNT-141 → QNT-144 (FINNHUB_API_KEY): Settings field added, `env_vars` allowlist + the `ASSET_REQUIRED_SETTINGS` test pin in `tests/dagster/test_run_launcher_env_vars.py` missed. Two-line fix: append `VERCEL_DEPLOY_HOOK_URL` to both. After CD landed, manually triggered `vercel_deploy_job` once to bring the frontend current rather than waiting for the next scheduled tick. Followups left out of scope: (1) flip `vercel_deploy.py:60-63` from "log INFO + return" to "raise in prod" so missing-config fails loud since the hook is now the only freshness mechanism (silent-success on a load-bearing path is the wrong default); (2) auto-derive `ASSET_REQUIRED_SETTINGS` by grepping `settings.X` references in `packages/dagster-pipelines/` rather than maintaining a manual allowlist that QNT-168 forgot to update.
- [ ] Adopt a state-tracking migration tool for ClickHouse — QNT-147
    - **Triggered by**: QNT-146 (PR #157) closed the manual-gate bug class by auto-applying `migrations/*.sql` on every deploy via curl + `IF NOT EXISTS`, but two convention-only safeguards remain that a single distracted PR can break: (1) editing a committed migration silently drifts dev vs prod (prod no-ops via IF NOT EXISTS, fresh dev runs the edited version); (2) a non-idempotent migration (DML, RENAME, complex ALTER) corrupts data on every deploy. A state-tracking tool (`golang-migrate` / `dbmate` / Atlas) catches both mechanically via `schema_migrations` table + content checksums. Adopt when ANY trigger fires: first DML migration, first non-additive ALTER, need to edit a committed migration, second ClickHouse instance, or 50+ migration files. Until then QNT-146's idempotent-by-convention is good enough.
- [x] CD: serialize prod deploys + namespace temp-file to fix parallel-deploy race — QNT-170
    - **Triggered by**: 2026-05-04 — QNT-160 + three Dependabot PRs merged in quick succession queued four CD runs in parallel against the same Hetzner host. Third run failed at "Restart services with changed bind-mounted configs" because `/tmp/equity-data-agent-prev-sha` was clobbered between concurrent runs. Two-part fix: (1) GitHub Actions concurrency group on the deploy workflow so prod deploys serialize, (2) namespace the temp-file with `${{ github.run_id }}` so even concurrent runs from different workflows don't collide.
- [x] Tame ClickHouse system-log creep (TTL on `text_log` / `trace_log` / `metric_log` / `asynchronous_metric_log`) — QNT-169
    - **Triggered by**: 2026-05-04 Grafana CPU panel triage — clickhouse-1 CPU climbing monotonically (0.07 → 0.18 cores over 5.5h, peak 72% during merge bursts) traced to system log tables with no TTL: `system.text_log` 44M rows / 19 active parts → 45.9M / 1.41 GiB by 2026-05-06, `trace_log` 18M → 21M / 334 MiB, `metric_log` 1.7M → 1.8M / 284 MiB, `asynchronous_metric_log` 275M → 303M / 127 MiB; text_log entry rate itself climbing 29.7k/hr → 41.4k/hr. **Re-scoped 2026-05-06**: the dozzle OOM half of the original ticket was already solved by QNT-164 / PR #195 (mem_limit 64m → 192m on 2026-05-03; live usage 12% of new limit); ticket retitled to ClickHouse-only. AC: 7d TTL on all four system log tables via clickhouse-config.xml, post-deploy verify row counts drop within retention + CPU baseline returns to ~0.05 cores, runbook entry added. Reference memory: `reference_clickhouse_system_log_merge_creep.md`.
- [x] obs-smoke: pre-prod assertion that every Prometheus target / Grafana panel / alert rule has data — QNT-172
    - **Triggered by**: Phase 7 retro (2026-05-06) — QNT-103 shipped clean against unit tests but landed 5 follow-up PRs in prod within 24h, none of which a unit test would catch by design (cAdvisor `--docker_only` flag missing, cAdvisor v0.56.x not in registry, node_exporter mountpoint label `/rootfs` vs `/`, CD didn't restart Grafana/Prometheus on provisioning changes, Dozzle OOM at 64m, Dozzle distroless healthcheck shape). All manifest as silent NoData / empty-panel / never-fires rather than loud failures. Same shape as `feedback_reactive_sizing_trap.md`, `feedback_health_endpoint_is_not_durability.md`, `feedback_aggregate_signals_hide_invariants.md` — shipped infra to prod and discovered it didn't work as advertised, with every signal still saying green. Adds `make obs-smoke` (or CI gate) asserting (1) every Prometheus target up, (2) every Grafana dashboard panel non-empty, (3) every alert rule state=ok-or-firing not unknown / no-data, (4) every CD-mounted config has a corresponding restart step in deploy.yml. Wired into CD as a hard gate after `docker compose up -d` and before `make check-prod`. Closes the structural gap that QNT-103's 5 reactive PRs were the parametric fix for.
- [x] Per-ticker news relevance filter at ingest — QNT-173
    - **Triggered by**: 2026-05-06 user spot-check on `/ticker/NVDA` — Finnhub `/company-news?symbol=X` returns whatever it tagged with X in `related` (sector roundups, peer-comparison pieces, one-line peripheral mentions), and `news_raw._article_to_row` wrote articles 1:1 into `equity_raw.news_raw` with no relevance gate. Same risk applies to every ticker; META and V most exposed because the symbol/name overlaps common English words ("metadata", lone-letter "V"). Fix: per-ticker `NEWS_RELEVANCE` dict in `shared.tickers` (alias list + `scope` of "any" vs "headline") with a module-load assert that keys match TICKERS, plus a strict word-boundary regex `(?<![-\w])alias(?![-\w])` in `news_raw._RELEVANCE_PATTERNS` that rejects hyphenated compounds (`meta-analysis`) which `\b` alone would let through — caught in code-review round 1, fixed in commit 2 with regression tests pinning both directions (drop hyphen-meta, keep "Meta's" possessive). Three tickers tuned by design: META scope=headline (lowercase "meta" too noisy in body prose), V drops the bare letter from aliases (matches `Visa` / `Visa Inc` only), GOOGL accepts both `GOOGL` and `GOOG`. Known limitations recorded inline in `tickers.py` (brand-after-hyphen drops like "Apple-designed silicon"; "Amazon River" body match at AMZN scope=any; "Musk" keeps SpaceX/X coverage under TSLA). Carry-forward post-deploy AC: post-tick ClickHouse query for ticker-vs-alias mismatch returns 0 rows; UI spot-check on NVDA/META/V shows relevant articles only and feeds non-empty.
- [x] TTM balance-sheet ratios + frontend ebitda/roe fix (QNT-179 round 2)
    - **Triggered by**: 2026-05-08 follow-up spot-check after PR #248 deployed and the AAPL Vercel page rebuilt — three more bugs surfaced on the Fundamentals card. (1) QUARTERLY tab: ROE/ROA rendered "--" even though `fundamental_summary` has them populated for the quarterly row. Frontend `roeRoaCell` deliberately read the latest TTM row's value instead, but `_build_ttm_rows` never put `roe`/`roa` in the TTM payload, so the fallback always resolved to null. (2) QUARTERLY tab: EBITDA MARGIN rendered 111.3% — frontend `pickEbitdaMargin` divided yfinance `info.ebitda` (point-in-time TTM ~$160B for AAPL, stamped on every period row by ingestion) by single-quarter revenue ($143.76B). The backend correctly leaves quarterly `ebitda_margin_pct` NULL because the math is meaningless; the frontend's client-side recompute then produced the bogus 111%. (3) TTM tab: GROSS MARGIN / DEBT-EQUITY / CURRENT RATIO / ROE / ROA all "--" — same root cause as (1), TTM payload only carried flow-of-funds ratios. Two-part fix: (1) `_build_ttm_rows` extends its payload with `roe = ni_ttm / equity_at_period_end * 100`, `roa = ni_ttm / total_assets_at_period_end * 100`, `gross_margin_pct = gross_profit_ttm / rev_ttm * 100` (introduces a rolling-4Q sum of `gross_profit` alongside the existing rolling sums), `debt_to_equity = total_debt / equity`, `current_ratio = current_assets / current_liabilities` — balance-sheet ratios pair TTM income with the snapshot at the matching quarter end, the canonical "ROE over trailing twelve months" financial dashboards expect. Negative book value is masked to NaN via `equity.where(equity > 0)` so structurally-negative-equity tickers (Boeing / SBUX class) render "--" instead of a misleading inverted ROE. (2) Frontend `pickEbitdaMargin` returns `null` for non-TTM rows (drops the misleading client-side division); `roeRoaCell` reads the row's own value for all period_types now that the backend populates them everywhere; stale "API now computes from BS values" comment removed; file-level docblock on the card rewritten to match the new behavior. Post-merge: re-materialize `fundamental_summary` for all 10 tickers so existing TTM rows pick up the new columns + Vercel rebuild via `dagster job execute -j vercel_deploy_job`.
- [x] Drop empty fundamental periods at ingestion instead of zero-filling — QNT-179
    - **Triggered by**: 2026-05-08 user spot-check — AAPL fundamental UI rendered empty (every ratio N/A, verdict "N/M (insufficient ratios)") even though 5 real quarters sat in `equity_raw.fundamentals` behind the empty top row. Root cause stacked two issues: (1) timing — weekly `fundamentals_weekly_schedule` ran Sun 2026-05-03 22:00 ET, two days after Apple's Q2 FY2026 earnings call (~May 1), at a moment yfinance had the column header for `2026-03-31` but cells were still NaN; (2) ingestion bug — `_safe_get` coerced NaN to `0.0`, indistinguishable from a real zero, so an all-zero stub row landed in CH and downstream `fundamental_summary` divided by zero, producing NULL ratios that the report template's `ORDER BY period_end DESC LIMIT 2` happily picked as "latest quarterly". Verified yfinance now (2026-05-08) returns full Q2 data; symptom self-heals on Sun May 10 via ReplacingMergeTree, but bug is latent. Two-part fix: (1) `_safe_get` now returns `None` on NaN/missing and a new `_safe_get_or_zero` wraps it for non-spine fields where the CH non-nullable Float64 columns force a zero-fill; `_extract_periods` skips a period whose `Total Revenue` is None so we never half-ingest a quarter; (2) new `fundamentals_no_all_zero_core_rows` asset check (severity WARN, non-blocking) flags any single row where revenue=0 AND net_income=0 AND total_assets=0 — the older `fundamentals_revenue_and_net_income_populated` only fired on all-zero tickers, missing single stubs mixed with real rows. Scope check found 8 historical stubs (GOOGL/JPM/META/TSLA/UNH for 2024-09-30 / 2024-12-31) yfinance never backfilled; they don't surface in the UI only because newer real rows shadow them. Backfill cleanup runs post-merge: `ALTER TABLE equity_raw.fundamentals DELETE WHERE revenue=0 AND net_income=0 AND total_assets=0` + re-materialize `fundamental_summary` for AAPL/GOOGL/JPM/META/TSLA/UNH. Code-reviewer-ediff advisory: bounded the new check's sample query with `LIMIT 11` so a future broader corruption can't pull an unbounded result set into the daemon.
- [x] Calibrate Quarterly fundamentals tab to use TTM for P/E, ROE, ROA, FCF yield — QNT-180
    - **Triggered by**: 2026-05-09 follow-up cell-walk after QNT-179 rounds 1-3 restored data integrity end-to-end. Four metrics on the QUARTERLY tab were technically correct but financially unusual on a single-quarter basis — NVDA Q4 FY2026 read P/E ANNUALISED 30.43 (price / Q-EPS×4), ROE 27.3%, ROA 20.8%, FCF yield 0.7%; every external dashboard (TradingView, Yahoo, stockanalysis.com, Bloomberg) quotes those four as TTM, where the canonical numbers are 43.55 / 76.3% / 58.1% / 1.8%. NVDA's lumpy Q4 made the ROE arithmetic gap (27 vs 76) visually jarring even though both rows were arithmetically valid. Pure frontend display calibration: the TTM row in `equity_derived.fundamental_summary` already carries `pe_ratio`, `roe`, `roa`, `fcf_yield` post-QNT-179 round 2. Three helpers in `fundamentals-card.tsx` reshaped to read from the latest TTM row when `period_type === "quarterly"`: `peCell` returns `{label: "P/E TTM", value: pe_ratio}` for both quarterly + ttm tabs (drops the `price / (Q-EPS × 4)` recompute on quarterly and the `price / EPS` recompute on TTM in favor of the backend value); `roeRoaCell` reads TTM with `(TTM)` value-suffix on quarterly and falls back to `—` if no TTM row; new `pickFcfYield` helper reads TTM `fcf_yield` on quarterly with the `(TTM)` marker placed after `yield` in `extraLabel` so the rendered order matches AC #5 (`1.8% yield (TTM)`). ANNUAL P/E branch unchanged (still `price / FY-EPS`). Code-reviewer-ediff advisory caught the FCF suffix ordering on round 1 — `extra="1.8% (TTM)"` + `extraLabel="yield"` would have rendered "1.8% (TTM) yield" instead of AC-specified "1.8% yield (TTM)"; fixed before push. ClickHouse cross-check via tunnel confirmed NVDA TTM @ 2026-01-31 `pe_ratio=43.55`, `roe=76.33`, `roa=58.06`, `fcf_yield=1.849`, quarterly `free_cash_flow=34.90B` — matches AC #5 exactly. No backend / data / DDL changes; single-file frontend diff.
- [x] Replace ephemeral cloudflared quick-tunnel with named Cloudflare tunnel — QNT-177
    - **Triggered by**: 2026-05-08 outage — Hetzner host rebooted at 04:00 UTC after `unattended-upgrades` installed a kernel CVE patch; cloudflared restarted with `--url http://api:8000` and got a fresh `*.trycloudflare.com` hostname; the Vercel build (SHA `7591bfa`, baked the previous URL into `NEXT_PUBLIC_API_URL` ten hours earlier) was now stale; `generateStaticParams()` fetched the dead URL at build time, returned `[]`, and `dynamicParams = false` collapsed every `/ticker/*` slug to 404 until manual recovery. Same root pattern as the Apr-18 reboot incident in memory; cadence is gated on Ubuntu kernel CVE releases (~3 weeks between events), not predictable. Fix: switch to a named Cloudflare tunnel anchored to `api.nusaverde.com` (domain already on Cloudflare for an unrelated site, so the cost objection in ADR-018's alternative B was moot). `cloudflared` service in `docker-compose.yml` now runs `tunnel --no-autoupdate run` reading `TUNNEL_TOKEN` from the environment (token in command-line was a `docker inspect`/`/proc/cmdline` exposure surface flagged by code-reviewer-ediff; env-only is the cloudflared-recommended pattern). Public hostname (`api.nusaverde.com → http://api:8000`) configured dashboard-side; pre-deploy `curl https://api.nusaverde.com/api/v1/health` returned CF 530 confirming the hostname mapping was live and waiting for the connector. Same PR removed the dormant `caddy` service + `Caddyfile` + `prod-caddy` profile + `caddy_data` volume (the named-tunnel path was always one of the two upgrade options listed in the original ADR-018; we picked it, and the Caddy fallback is no longer load-bearing). ADR-018 status flipped to Superseded with a full Update section recording trigger, decision, consequences, and reversibility. Vercel `NEXT_PUBLIC_API_URL` becomes permanent (`https://api.nusaverde.com`) — no more refresh-on-rotate runbook. **Code-reviewer-ediff caught 3 BLOCKING issues pre-merge** — fixed in the same PR: (1) tunnel token was in `--token ${VAR}` flag → leaked via `docker inspect .Config.Cmd`, switched to `TUNNEL_TOKEN` env-only; (2) `scripts/obs_smoke.py` + `tests/test_obs_smoke.py` retained `prod-caddy` references as dormant-profile machinery (test asserted vacuously after caddy deletion); rewrote the comment generically and dropped the now-content-free test; (3) `.claude/agents/ops-investigator.md` listed `caddy` in the compose stack — would have made the ops-investigator agent attempt to inspect a non-existent container during prod triage; replaced with `cloudflared`. Advisory sweep: also updated `docs/guides/hetzner-bootstrap.md` (§5 caddy steps) and `docs/project-requirement.md` (deploy verify URLs) to point at `api.<your-domain>` instead of trycloudflare; original Phase 6 narrative paragraphs in project-requirement.md left as historical context. Reference memory: same class as `feedback_health_endpoint_is_not_durability.md` (Apr-18) — rolling kernel-update reboots will keep happening; the fix is structural anchoring, not faster recovery.

---

### Phase 1 — Data Ingestion
**Scope**: Dagster assets that fetch and store OHLCV + fundamental data. Batch-only — no streaming.

**Ingestion Strategy**:
- **Partitioning**: `StaticPartitionsDefinition` by ticker (10 partitions). Per-ticker visibility, retry, and parallel execution in Dagster UI. **Max 3 concurrent partitions** to avoid yfinance rate limiting during backfill (configured via Dagster `TagConcurrencyLimit`).
- **Backfill**: One-time materialization of all partitions with `period="2y"` (2 years of history). Enough for all technical indicators and YoY comparisons. Triggered via manual materialization in Dagster UI with `period="2y"` asset config.
- **Incremental (daily OHLCV)**: Fetch last 5 trading days per ticker, `ReplacingMergeTree` deduplicates. No need to track "last fetched date." The daily schedule hardcodes `period="5d"` via `RunConfig`.
- **Incremental (weekly fundamentals)**: Fetch all available quarters (yfinance returns last 4 quarterly + 4 annual), `ReplacingMergeTree` deduplicates.
- **Schedule**: Daily OHLCV at ~5-6 PM ET (after market close, data settles). Weekly fundamentals on weekends.

**Deliverables**:
- [x] Implement `ohlcv_raw` Dagster asset (yfinance → ClickHouse) — QNT-41
    - `StaticPartitionsDefinition` by ticker
    - Backfill: `period="2y"`, Incremental: `period="5d"`
    - Rate limiting: 1-2s sleep between tickers, exponential backoff on 429s
- [x] Implement `fundamentals` Dagster asset (yfinance → ClickHouse) — QNT-42
    - `StaticPartitionsDefinition` by ticker
    - Fetches all available quarterly + annual data each run
- [x] Add Dagster schedules: daily for OHLCV (~5-6 PM ET), weekly for fundamentals — QNT-43
- [x] Implement Dagster resource for ClickHouse client (shared across assets) — QNT-40
- [x] Verify: Run backfill for all 10 tickers, confirm data in ClickHouse, check Dagster lineage graph — verified 2026-04-19 in prod: `ohlcv_raw` has 504 rows/ticker (2024-04-15 → 2026-04-17) for all 10 tickers; `fundamentals` has 9-11 quarters/ticker; derived tables populated (weekly=1040, monthly=240, tech_daily=5040, fund_summary=101); Dagster asset graph loads 8 assets + 17 checks + 2 schedules + 2 sensors

---

### Phase 2 — Calculation Layer
**Scope**: Technical indicators, fundamental ratio computation, and multi-timeframe aggregation.

- [x] Implement `ohlcv_weekly` and `ohlcv_monthly` Dagster aggregation assets — QNT-70
- [x] `ohlcv_weekly`:
    - Reads from `ohlcv_raw`, aggregates daily bars → weekly (Monday-based) OHLCV
    - Aggregation via pandas groupby (`toMonday(date)`): open=first, close=last, adj_close=last, high=max, low=min, volume=sum
    - **Skip the current incomplete week** — only emit bars for weeks where the last trading day has passed (avoids partial bars that would distort indicators)
    - Downstream dependency on `ohlcv_raw` asset
- [x] `ohlcv_monthly`:
    - Reads from `ohlcv_raw`, aggregates daily bars → monthly OHLCV
    - Same aggregation logic (open=first, close=last, adj_close=last, high=max, low=min, volume=sum) with `toStartOfMonth(date)` grouping
    - **Skip the current incomplete month** — same rationale as weekly
    - Downstream dependency on `ohlcv_raw` asset
- [x] Implement `technical_indicators` Dagster assets (daily, weekly, monthly) — QNT-44
    - Computes RSI-14, MACD (12/26/9), SMA-20/50, EMA-12/26, Bollinger Bands (20,2)
    - **Price input: `adj_close`** — use adjusted close to avoid false signals at stock split boundaries
    - **Warm-up**: indicators are `null` until enough prior data exists (RSI-14: 14 rows, SMA-50: 50 rows, MACD signal: 35 rows). Rows are still written with nulls — FastAPI and frontend handle display.
    - Same indicator code, three input sources: `ohlcv_raw`, `ohlcv_weekly`, `ohlcv_monthly`
    - Writes to `technical_indicators_daily`, `_weekly`, `_monthly`
    - Uses pandas/numpy — all math in Python, never in the LLM
- [x] Implement `fundamental_summary` Dagster asset (15 ratios) — QNT-45
    - **Valuation**: P/E, EV/EBITDA, P/B, P/S, EPS
    - **Growth**: revenue YoY%, net income YoY%, FCF YoY%
    - **Profitability**: net margin%, gross margin%, ROE, ROA
    - **Cash**: FCF yield
    - **Leverage**: D/E
    - **Liquidity**: current ratio
    - Downstream dependency on BOTH `fundamentals` AND `ohlcv_raw` — price-based ratios (P/E, P/B, P/S, FCF yield) require latest close price from `ohlcv_raw`
- [x] Add Dagster sensors to trigger downstream recomputation when raw data refreshes — QNT-46
    - `ohlcv_raw` materialization → triggers `ohlcv_weekly`, `ohlcv_monthly`, `technical_indicators_daily`, `fundamental_summary`
    - `fundamentals` materialization → triggers `fundamental_summary`
    - This means price-based ratios (P/E, P/B, P/S, FCF yield) update daily with fresh close prices, while statement-based ratios (margins, growth) update weekly with fresh fundamentals
- [x] Add Dagster asset checks for data quality validation — QNT-68
    - e.g., no NaN close prices, volume > 0, RSI within 0-100, no future dates
- [x] Null out P/E when EPS is near zero (|EPS| < $0.10) to honor N/M convention — QNT-87
    - **Triggered by**: Phase 2 QA — P/E was emitting absurd values (>1000x) during near-zero-EPS quarters, pretending precision where the ratio is meaningless. Now renders as `N/M (near-zero earnings)` in reports.
- [x] Fix P/E to use TTM earnings on quarterly rows in fundamental_summary — QNT-91
    - **Triggered by**: Phase 2 QA — quarterly P/E was dividing price by single-quarter EPS, inflating the ratio by ~4×. Switched to trailing-twelve-month EPS so quarterly and annual P/E are directly comparable.
- [x] Set `default_status=RUNNING` on all sensors and schedules — QNT-92
    - **Triggered by**: Runtime-state drift — Dagster defaults sensors/schedules to STOPPED, requiring a manual UI toggle after every redeploy. Declaring `RUNNING` in code makes prod runtime state fully reproducible from git.
- [x] Validation tests: indicators vs external sources — QNT-47
    - Snapshot tests with fixed datasets and expected outputs
    - Cross-reference RSI, MACD, P/E for 2-3 tickers against TradingView / Yahoo Finance
    - Tolerance: 1% for technical indicators, exact match for fundamental ratios; fixtures committed for determinism
- [x] Verify: Run full pipeline Raw → Aggregation → Indicators, spot-check calculations against external sources (e.g., TradingView) — covered by QNT-47 (canonical Wilder/Appel cross-reference tests) + QNT-68 asset checks + `docs/retros/phase-2-ac-audit.md`

---

### Phase 3 — API Layer
**Scope**: FastAPI endpoints serving machine-readable data (frontend charts) and human-readable reports (agent).
**Dependencies**: Requires Phase 2 (data must exist in ClickHouse). Can proceed in parallel with Phase 4 — news endpoints gracefully degrade to empty responses until Phase 4 populates `news_raw`.

**Report template — build this FIRST (QNT-69):**
- [x] Design **one** report template end-to-end against real ClickHouse data — QNT-69 **[start of Phase 3]**
    - **Target: the technical report** (`/reports/technical/{ticker}`). Build the full pipeline — query CH → format into a report string → expose at the endpoint — against live Phase 2 data. Iterate with eyes on the actual output until it reads well. THEN parameterise the pattern for fundamental / news / summary.
    - **Rationale**: the templates are where the "intelligence vs math" thesis actually lives in the product — they determine what the agent can reason over. Parameterising a bad template 4 times is waste; finding the right shape once and then applying it is not.
    - Structured sections (not walls of text), comparative context ("RSI 72.3 — above 70, approaching overbought"), historical context ("Revenue grew 23% YoY, accelerating from 18%"), explicit signal clarity (bullish / bearish / neutral).
    - **Null/N/M display conventions** (Phase 2 retro finding): P/E nulled when `|EPS| < $0.10` → "N/M (near-zero earnings)", quarterly P/E uses TTM net income, indicator warm-up nulls → "Insufficient data (N bars required)". These conventions apply to all report endpoints.
    - Templates stored under `packages/api/src/api/templates/` or as formatter functions in services.

**Report endpoints (text — for the agent; all apply the QNT-69 template pattern):**
- [x] `GET /api/v1/reports/technical/{ticker}` — formatted text report with indicator context — QNT-48 *(first concrete output of QNT-69)*
- [x] `GET /api/v1/reports/fundamental/{ticker}` — formatted text report with ratio context — QNT-49
- [x] `GET /api/v1/reports/news/{ticker}` — recent news summary. Depends on Phase 4 `news_raw` data — returns 200 with a well-formed text report containing an `N/M (no news ingested…)` block until Phase 4 populates data. Sentiment narrative lands when QNT-55 (Qdrant search) ships. — QNT-79
- [x] `GET /api/v1/reports/summary/{ticker}` — combined text overview: latest price context, RSI interpretation, trend narrative, and sector context. Sector context derived from a static mapping in `shared/tickers.py`. Used by the agent as a quick "at a glance" tool. — QNT-50

**Data endpoints (JSON — for the frontend):**
- [x] `GET /api/v1/ohlcv/{ticker}?timeframe=daily|weekly|monthly` — returns `[{time, open, high, low, close, adj_close, volume}]` for TradingView chart rendering. `time` is an ISO date string `"YYYY-MM-DD"` — QNT-76
- [x] `GET /api/v1/indicators/{ticker}?timeframe=daily|weekly|monthly` — returns `[{time, rsi_14, macd, macd_signal, macd_hist, sma_20, sma_50, ema_12, ema_26, bb_upper, bb_middle, bb_lower}]` as row-oriented time-series (`null` during indicator warm-up period) — QNT-77
    - **Warm-up periods**: RSI-14: 14, EMA-12: 12, EMA-26/MACD/MACD signal: 35, SMA-20/BB: 20, SMA-50: 50. All non-null from row 50 onward.
- [x] `GET /api/v1/fundamentals/{ticker}` — latest fundamental ratios as structured JSON for the ticker detail page ratios table — QNT-80
- [x] `GET /api/v1/dashboard/summary` — returns `[{ticker, price, daily_change_pct, rsi_14, rsi_signal, trend_status}]` for ALL tickers in a single response. Avoids N+1 requests on dashboard load. — QNT-81
    - `price`: latest available `close` from `ohlcv_raw` (real market price, NOT `adj_close`)
    - `daily_change_pct`: `(latest_close - prev_close) / prev_close * 100` — trivial presentation arithmetic (see §2.1)
    - `rsi_signal`: `"overbought"` (RSI > 70), `"oversold"` (RSI < 30), `"neutral"` (30–70)
    - `trend_status`: `"bullish"` (close > SMA-50), `"bearish"` (close < SMA-50), `"neutral"` (warm-up)

**Utility endpoints:**
- [x] `GET /api/v1/tickers` — returns the ticker list from `shared.tickers.TICKERS` — QNT-78
- [x] `GET /api/v1/health` — health check with ClickHouse + Qdrant connectivity status + deploy identity (git SHA, Dagster asset/check counts) — QNT-51

**Cross-cutting:**
- [x] CORS middleware configured (allow production domain, `*.vercel.app` for preview deploys, and `localhost:3001` for dev) — `packages/api/src/api/main.py:131-137`
- [x] Ticker validation: all `{ticker}` path endpoints AND the `POST /agent/chat` request body validate the ticker against `shared.tickers.TICKERS` and return `404 {"detail": "Ticker not found"}` for unknown tickers — enforced in `packages/api/src/api/routers/data.py` and all report template formatters
- [x] No API authentication in initial scope — the API is read-only and serves public market data
- [x] Verify: Hit all endpoints with VS Code REST Client (`.http` files), confirm chart data arrays are correctly structured, check OpenAPI docs at `/docs` — verified 2026-04-19 via prod `curl` pass: 10 OpenAPI paths, correct row counts (504 daily / 104 weekly / 24 monthly / 10 fundamental / 10 dashboard), all `{ticker}` endpoints 404 on BOGUS, `/docs` 200

---

### Phase 4 — Narrative Data
**Scope**: News ingestion, embedding, and semantic search via Qdrant.

- [x] Ingest news via **RSS + `feedparser`** — QNT-52
    - Per-ticker Yahoo Finance RSS (`https://feeds.finance.yahoo.com/rss/2.0/headline?s={ticker}&region=US&lang=en-US`), plus 1–2 broad market feeds (e.g., Reuters markets RSS)
    - No paid news API evaluation — RSS is free, unrate-limited, and deterministic enough for a 10-ticker scope. The news-API comparison rabbit hole is not the portfolio story; RSS + embeddings + semantic search is.
- [x] Implement `news_raw` Dagster asset (RSS feeds → `equity_raw.news_raw` in ClickHouse) — QNT-53
    - Schedule: every 4 hours during market hours, `default_status=RUNNING` (Phase 2 lesson: QNT-92)
    - Dedup key: `id = hash(ticker + url)`
    - Stores: `headline`, `body`, `source`, `url`, `published_at` per ticker
    - Downstream sensor (`news_raw` → `news_embeddings`) must batch all pending events per tick from day one (Phase 2 lesson: QNT-46 rewrite) — **deferred to QNT-54** (target asset `news_embeddings` lives there; reuses the existing `_build_materialization_sensor` factory for batch semantics)
- [x] Create Qdrant `equity_news` collection (384-dim Float32, cosine distance) — auto-create in the Qdrant Dagster resource on first use, or via a setup script (shipped as part of QNT-54 — `QdrantResource.ensure_collection` runs at asset-start)
- [x] Implement `news_embeddings` Dagster asset (`news_raw` → Qdrant Cloud) — QNT-54
    - Embeds `headline` text using `sentence-transformers/all-MiniLM-L6-v2` (384-dim)
    - Sensor-triggered when `news_raw` materializes new rows
    - Stores vector + full payload (headline, source, url, published_at, ticker) in Qdrant
    - **Tests use a fake Qdrant client** (analogous to the `_FakeClient` for ClickHouse in `packages/api/tests/test_data.py`) so CI is deterministic with no Qdrant Cloud connection — Phase 3 lesson
- [x] `GET /api/v1/search/news?ticker=NVDA&query=earnings` — QNT-55
    - Returns `[{headline, source, url, published_at, score}]` — top-N results ranked by cosine similarity
    - Both `ticker` and `query` are required. Returns `[]` if Qdrant is unreachable or no news data exists.
    - **Tests use a fake Qdrant client** (same `_FakeClient` pattern) covering ranking, payload shape, and the empty-results/unreachable fallback — Phase 3 lesson
- [x] Add Dagster asset checks for `news_raw` and `news_embeddings` data quality — QNT-93
    - `news_raw`: no empty headlines, valid URLs, no future `published_at` dates, row count per ticker
    - `news_embeddings`: vector count matches source row count, no orphaned vectors
    - Phase 2 lesson: QNT-68 asset checks caught 2 real formula bugs — apply same pattern to news assets
- [x] Namespace Qdrant point IDs by ticker — QNT-120
    - **Triggered by**: QNT-93 `news_embeddings_vector_count_matches_source` flagged every ticker's `|qdrant − clickhouse| > tolerance` on 2026-04-23. Root cause: `news_embeddings` keyed Qdrant points by `blake2b(url)`, but `news_raw` keys rows by `(ticker, url)` — so cross-mentioned URLs had the last ticker's upsert win and silently disappeared from per-ticker search.
    - Fix: `point_id(ticker, url_id) = blake2b(f"{ticker}:{url_id}")` restores 1:1 mapping to ClickHouse's composite key. Orphan asset check updated to compute expected namespaced IDs in Python.
    - Migration: drop `equity_news` collection post-deploy (not backwards-compatible); next sensor tick re-embeds from the 7-day window. Script: `scripts/drop_qdrant_news_collection.py --yes`.
    - Reinforces `feedback_asset_checks_catch_real_bugs.md` + `feedback_dont_explain_away_first_warn.md` — investigated the first prod WARN rather than writing it off as backlog.
- [ ] Verify: Search for recent news about a ticker, confirm relevance ranking

---

### Phase 5 — Agent Layer
**Scope**: LangGraph agent with tools that call FastAPI endpoints.

- [x] Configure LiteLLM proxy via `litellm_config.yaml` — QNT-59
    - **Default**: routes to Groq (`https://api.groq.com/openai/v1`, llama-3.3-70b-versatile) via `GROQ_API_KEY`. Email-only free tier (30 RPM / 6K TPM / up to 14.4K RPD) covers Phase 5 dev + steady-state portfolio demos. ~500 tok/s inference keeps prompt-iteration fast.
    - **Override**: routes to Google AI Studio Gemini 2.5 Flash via `GEMINI_API_KEY` — free-tier quality override (15 RPM / 1500 RPD, no credit card) for README screenshots (QNT-66 / QNT-139) and as the per-provider axis in the QNT-67 eval harness.
    - Model alias: `equity-agent/default` — zero agent code changes to switch backends.
    - See ADR-011 for provider selection rationale (why Groq over Ollama Cloud / Gemini / OpenAI / self-hosted).
- [x] Demote Gemini override from Pro to Flash — QNT-123
    - Shipped right after QNT-59. First live test of the `equity-agent/gemini` alias returned HTTP 429 with `limit: 0` — Gemini 2.5 Pro is no longer on Google AI Studio's free tier. Swapped to Gemini 2.5 Flash (free-tier-reachable) to preserve ADR-011's "free to clone" invariant. Pro stays available via one-line YAML edit if a paid plan is ever added. See ADR-011 §Revision history (2026-04-23 Pro → Flash entry) for the full story.
- [x] Integrate Langfuse tracing — QNT-61 **[day-one of Phase 5, moved from Phase 7]**
    - `LangfuseResource` in the agent package; `@observe` decorator on every tool and graph node from the first commit of agent code — traces are needed *while* iterating on the prompt, not bolted on after shipping.
    - Portfolio artifact: one Langfuse trace screenshot is embedded in the README (QNT-66).
- [x] Define LangGraph state schema (ticker under analysis, gathered reports, thesis draft) — QNT-56
- [x] Implement tools — QNT-57
    - `get_summary_report` → calls `/reports/summary/{ticker}` (agent calls this first)
    - `get_technical_report` → calls `/reports/technical/{ticker}`
    - `get_fundamental_report` → calls `/reports/fundamental/{ticker}`
    - `get_news_report` → calls `/reports/news/{ticker}`
    - `search_news` → calls `/search/news`
    - **Ship as one PR using the template-pattern approach from QNT-69**: all 5 tools share the same shape (`httpx.get(url).text → return string`) with only the URL template varying. Build the hardest tool end-to-end first (`search_news` — has extra `query` arg + empty-results fallback), then parameterise for the other 4 in the same PR. Resist splitting into 5 PRs. Phase 3 lesson (PR #53 bundled QNT-69 + 4 report endpoints).
    - **Pre-implementation tool-contract block (required in PR body)** — for each tool, a three-line mini-spec before any code:
        - **Input**: named args + types (`ticker: str`, `query: str`, etc.)
        - **Upstream**: exact FastAPI method + path (`GET /api/v1/reports/technical/{ticker}`)
        - **Return**: concrete shape (e.g., "plain-text report — numbers appear verbatim; consumed by LLM as-is") and the degraded-case return (`""`, `"No news data available."`, etc.)
      Forces the API-response ↔ graph-state identity to surface at design time. Phase 4 retro lesson: QNT-54 shipped a Qdrant point-ID scheme that silently collided with ClickHouse's composite key because the cross-store identity was never written down (cost: QNT-120 fix + follow-up). Apply the same identity-first discipline to agent tools. Reinforces ADR-003 by naming *where* every number in the thesis originates.
- [x] Build agent graph — **3 nodes: plan → gather → synthesize** (per ADR-007) — QNT-56
    - No critique / reflect / retry loop until the baseline has failed in specific, observed ways. Adding loops prematurely is indistinguishable from the baseline working.
- [x] System prompt enforcing the "interpret, don't calculate" mandate — QNT-58
    - **Triggered by**: Phase 5 Agent Layer architectural-boundary work — ADR-003's "LLM never does math, only interprets reports" had been paraphrased into a one-liner buried in a graph-node closure, which drifted each time the prompt changed. This ticket promotes the four issue-body rules (no arithmetic / cite source / structured thesis / confidence anchored to data completeness) to a named `SYSTEM_PROMPT` in `packages/agent/src/agent/prompts/` and wires them into the synthesize node. Round-1 review caught three issues that would have meaningfully degraded the contract: (1) the prompt was being delivered as a flat user-turn string via `langchain.ChatModel.invoke(str)` rather than a `SystemMessage`, dropping ADR-003's authority on most providers; (2) the `=== <name> report ===` fences for report bodies were vulnerable to fence-collision injection from FastAPI report content (news headlines etc.); (3) `REPORT_SOURCES` duplicated the graph's `REPORT_TOOLS` tuple with only a runtime-test guard against drift. Round-2 fixes: `build_synthesis_prompt` returns `[SystemMessage(SYSTEM_PROMPT), HumanMessage(...)]`, `traced_invoke` accepts `str | list[BaseMessage]`, `_sanitize_report_body` neutralises `===` runs in untrusted bodies (replace with `==·==`, with parametrized tests pinning the long-run invariant so a future "simplify" refactor can't reintroduce the gap), and `REPORT_TOOLS` lives canonically in `agent.prompts.system` with `is`-identity tested. Empirical "model obeys the rules in actual output" verification is QNT-67's defining job (hallucination eval) — Phase 5 splits "set the rule" (this ticket) from "measure compliance" (next ticket) by design.
- [x] Agent CLI: `python -m agent analyze NVDA` — run single-ticker analysis from terminal — QNT-60
    - **Built before the SSE endpoint** — ~50× faster prompt iteration without a frontend round-trip. Pairs with the eval harness below.
- [x] Surface canonical fundamental thresholds in the fundamental-report template (parameterised QNT-136) — QNT-137
    - **Triggered by**: QNT-136 ship + sweep `20260426T085600Z-9433e1`. After the TA-side fix landed, the same leak class re-emerged on the fundamental side — `27.5`, `50`, `100`, `40`, `15` showing up as `unsupported` because `_signal_verdict` votes against P/E (< 20 / > 40), revenue YoY (> 10 / < 0), net margin (> 15 / < 0), and ROE (> 15 / < 0) but the report body never printed those numbers. Same root cause, different surface, exactly the open question ADR-012's "negative / open questions" section flagged. Fix is the parameterised application of ADR-012: `_pe_label` annotates every P/E line (including N/M) with `(rich ≥ 40, cheap ≤ 20)`; `_GROWTH_REFERENCE = "Reference rates: ≥ 10% strong, ≤ 0% contraction"` is unconditionally appended to the GROWTH section; `_PROFITABILITY_REFERENCE` covers net margin + ROE 15%/0 thresholds in the same shape. Six new parametrised tests in `test_templates.py` mirror QNT-136's `test_technical_rsi_label_always_cites_canonical_thresholds`. Empirical: 3 post-fix sweeps (run IDs `6672bc`, `a6020a`, `045041`) average 14.33/16 hallucination_ok (15/14/14), with `27.5, 100, 40` absent across 48 records and `15` / `50` reduced to single-occurrence flakes inside ADR-012's variance carve-out. **Known out-of-scope residual**: `jpm-fundamental` fails 3/3 with company-specific number leakage (`148.31, 155.23, 200, 70, 62, 155.00, 142.50`) — distinct sub-class from threshold leakage; pre-existed QNT-137 and is not what this ticket addressed. EV/EBITDA / Price/Book / Price/Sales were left unannotated per the ticket's "sector-relative, may not have a clean canonical pair" hedge — they don't appear in `_signal_verdict` so there's no leak surface today. Calibration note: this is the exact follow-up ADR-012 anticipated, so the fix landed within hours of the originating sweep; classify-bug-variant memory paid off (the JPM failure looked superficially like the same class but the unsupported numbers belong to a company-specific sub-class, distinct from the convention-leak class QNT-137 addresses).
- [x] Surface canonical TA thresholds in the technical-report template so the agent quotes them verbatim — QNT-136
    - **Triggered by**: QNT-129 prep — running the QNT-67 sweep on the post-QNT-133 prompt as a precursor to the model bench surfaced two regressions on the same axis. (1) The QNT-133 prompt I shipped yesterday carried `RSI > 75` as a verdict-action example; the model parroted "75" into 3/16 theses where the technical report didn't contain 75 (the literal number bled from the schema description that `with_structured_output` injects). (2) After dropping the prompt literal, the leak class shifted to `70` / `80` — the model reaching for canonical RSI overbought/oversold thresholds when the report didn't print them (NVDA's report cites "above 70 threshold" so it passed; GOOGL's report says "approaching overbought" with no number, so the model leaked "70" from prior knowledge). The category error was on the scorer's side: `RSI > 70 = overbought` is canonical TA convention, not a fact-claim about a specific company — but the hallucination scorer can't tell those apart without help. ADR-012 picks Option B (surface conventions in the report layer, not loosen the prompt or the scorer) and parameterises `_rsi_label` so every bucket prints both 70 and 30 in the report body. Empirical: pre-fix run `20260426T082316Z-d8d42e` showed leak set `{70, 80}` on TA records; post-fix canonical baseline `20260426T091357Z-625e57` shows all `*-technical` records clean and residual leakage on the fundamental/news side (tracked as QNT-137). New baseline retires the QNT-128 16/16 mark — the QNT-133 prompt's mandatory Action line raises the numeric-claim surface area on every thesis, so 12-14/16 with TA-class clean is the honest floor. Side guards: a regression test pins "no multi-digit literals in `SYSTEM_PROMPT`"; a sibling pins the same on every `Thesis.model_fields` description (because `with_structured_output` injects descriptions into the LLM's schema, making them a parallel bleed surface to the prompt body itself). Calibration note: the over-tightened prompt iteration (run `20260426T083425Z-f50e0f` adding "do not write canonical thresholds" rules) collapsed judge from 6.5 → 2.94 with no hallucination improvement — confirming the right layer for the fix is the report, not more rules at the prompt layer.
- [x] Restructure thesis output to Setup / Bull Case / Bear Case / Verdict — QNT-133
    - **Triggered by**: Phase 6 design v2 (TERMINAL/NINE) thesis card. The QNT-58 prompt produced a 5-section narrative (Overview / Technical / Fundamental / News / Conclusion) that read like a checkbox-completed report rather than a research note with a stance. QNT-133 swaps the prompt + state contract: synthesize now uses `with_structured_output(Thesis)` (a Pydantic model with `setup: str`, `bull_case: list[str]`, `bear_case: list[str]`, `verdict_stance: Literal[constructive|cautious|negative|mixed]`, `verdict_action: str`), so the four sections reach the API as typed JSON instead of prose the frontend has to regex. Two design-side risks were locked into the prompt rather than left for the model: (1) **forced symmetry** — bull and bear sections explicitly allow EMPTY when the supplied reports don't argue for that side (verified live: TSLA produced 0 bull / 3 bear, stance=negative, action="no action level supported by current data"); (2) **hallucinated action levels** — verdict guidance must reference numbers verbatim from the technical/fundamental reports, never invented price targets (verified live: V cites SMA-20 307.32 + RSI-14 49.7, UNH cites SMA-50 292.81, AAPL cites SMA-50 260.15 — all from the technical-report body the agent gathered). The CLI keeps its markdown contract via `Thesis.to_markdown()`; the eval harness renders the same way so QNT-67's hallucination scorer keeps pattern-matching numbers without changes. Side fix: the `test_no_raw_llm_invoke_in_agent_package` AST scanner only walked the top-level RHS for `get_llm()`, so a chained `structured = get_llm().with_structured_output(...)` binding would have let `structured.invoke(...)` slip past CI; reviewer-agent caught the gap, scanner now `ast.walk`s the full RHS and two regression fixtures pin the new pattern. Calibration note: when `traced_invoke`'s output handling was widened to JSON-dump pydantic responses, the naive `isinstance(response, BaseModel)` matched `AIMessage` first (langchain messages ARE pydantic BaseModels) and broke an existing tracing assertion — order matters: check `BaseMessage` first, then `BaseModel` for the schema-instance fallback.
- [x] Agent evaluation framework — QNT-67 **[highest-priority Phase 5 item — the single biggest AI-Engineer hiring signal]**
    - Lives under `packages/agent/evals/`. Three eval types — all required, not optional:
    - **(a) Numeric-claim hallucination detector** (`evals/hallucination.py`): regex every number out of the agent's thesis; assert each appears verbatim in one of the report strings the agent received as tool output. Any mismatch = test failure. Operationalises the ADR-003 contract.
    - **(b) Golden set** (`evals/golden_set.py` + `evals/goldens/questions.yaml`): 15–20 curated `(ticker, question, reference_thesis, expected_tools)` pairs. **Coverage invariant: at least one question per ticker in `shared.tickers.TICKERS`** — thin, heavy, volatile, and quiet tickers all exercise the pipeline. Phase 2 retro lesson (`feedback_sample_ac_broadly.md`): a golden set that skews to NVDA/AAPL leaves bugs in UNH/V/JPM invisible to regression. Per run, track LLM-as-judge score + cosine similarity of generated thesis vs reference thesis. Commit `evals/history.csv` so prompt-version quality is visible in `git log -p`.
    - **(c) Tool-call correctness** (`evals/tool_calls.py`): for each golden-set question, assert the expected tool was called — e.g., valuation questions MUST call `get_fundamental_report`, technical questions MUST call `get_technical_report`.
    - Design goal: harness is reusable enough to extract as a standalone repo later.
- [x] Investigate + fix the 3 hallucination findings the QNT-67 baseline flagged — QNT-128
    - **Triggered by**: QNT-67 baseline (commit 1b66e7b, run 20260425T092008Z) flagged 3 records emitting numbers absent from any tool-output report — `amzn-fundamental` (16.09), `unh-fundamental` (89.02 + 99.82), `unh-news` (same UNH numbers). The framework was doing exactly what it should; per the issue body the next step was to classify each finding as (a) rounding / (b) cross-pollination / (c) pretraining leakage / (d) regex false-positive, then apply the matching lever. Replay of all 3 records showed every "hallucinated" number was actually present in the reports as the negative form (`-16.09`, `-89.02`, `-99.82`) — the reports template YoY changes with explicit signs while the model naturally moves the sign into English verbs ("free cash flow declined 16.09%"). Class (d): regex false-positive. Fix: support comparison strips the leading sign before lookup; canonical form (and the `unsupported` listing for `--explain`) keeps it for explainability. Trade-off documented in `hallucination.py`'s "Sign-magnitude support" section and pinned by an `xfail(strict=True)` tripwire (`test_inverted_sign_thesis_should_be_flagged_but_is_not`) — if a future change re-introduces asymmetric sign comparison without a deliberate decision to remove the trade-off, the suite fails loud. Verifying live sweep (run 20260425T142759Z): 16/16 hallucination_ok, 16/16 tool_call_ok, exit 0. Side effect: `litellm_config.yaml` adds Qwen3-32B as auto-fallback for the default Llama-3.3-70B alias (Groq's free-tier daily TPD on Llama is 100K, ~2 sweeps' worth; Qwen-fallback adds 5× more headroom). Llama stays default — Qwen-as-default was tested first and regressed 15/16 → 11/16 hallucination_ok because Qwen rounds numbers and leaks `<think>` reasoning into the thesis content.
- [x] Re-run Llama-3.3-70B bench on a fresh TPD bucket — QNT-138
    - **Triggered by**: QNT-129 follow-up — the production-default `bench-llama3-70b` row in the bench was anchored on a partially-throttled TPD bucket (Groq daily token-per-day cap was already mid-burn from prep sweeps when the canonical run fired), so the reference line carries 7 zero-cosine records from failed syntheses. The bench doc footnotes this as "0.336 (clean) / 0.189 (raw)" — fine for the first publish, but every future model decision references back to that row and a clean reference is worth the rerun. Empirical: clean re-run on `equity-agent/bench-llama3-70b` (run_id `20260427T122411Z-8ebb34`) lands 16/16 hallucination_ok, 16/16 tool_call_ok, judge 4.69, **cosine 0.364** — the highest cosine in the bench, ahead of Scout's 0.342. The pre-registered decision rule (Scout cosine lead ≥ 0.02 OR judge lead ≥ 2.0 → promote) fires on neither: cosine lead is **negative** (-0.022, Llama leads), judge lead is +0.25. Outcome: keep Llama-3.3-70B as default, leave the QNT-129-shipped chain (`equity-agent/default` → `equity-agent/fallback-llama4scout`) intact. The `Two weeks after this PR ships` Scout-promotion gate is removed from the doc's revisit cadence — the question is closed on data, not waited out. Side product: aggregate + token-summary scripts promoted from `/tmp/` into `scripts/evals/` (the doc's "second re-run" trigger fired); aggregate now defaults to latest-sweep-per-alias so the original TPD-truncated sweep + the QNT-138 NVDA probe are both retained in `history.csv` as audit trail without contaminating the doc-publishable numbers. Calibration note: the wall-clock p50 of 8 398 ms looks bad next to Scout's 836 ms but is mostly the per-alias bench TPM cap (5 K TPM on `bench-llama3-70b`) queueing late-record plan calls — per-call Langfuse latency was 311 ms, ~2× Scout's 169 ms (real but not the 10× the wall-clock implies); the production default has no such cap, so the 2× per-call edge is the only signal that survives a clean comparison and isn't enough to disrupt months of production track record.
- [x] Bench free-tier candidate models against QNT-67 goldens (portfolio artifact) — QNT-129
    - **Triggered by**: QNT-128 retro lesson — the Qwen3-32B fallback was promoted on a *capacity* signal (5× the default's TPD) without checking quality first; only after the fact did a re-run on the goldens find Qwen had regressed 15/16 → 11/16 hallucination_ok. Wrong sequence (capacity-then-quality instead of quality-then-capacity) and exactly the gap a portfolio reader of `litellm_config.yaml` would notice on inspection. This ticket establishes a repeatable bench whose output is the artifact every future model-selection decision references back to. Plumbing: 7 dedicated `equity-agent/bench-<model>` aliases in `litellm_config.yaml` plus an 8th `bench-llama3-70b` reference alias added during the run (so the production model gets a fair within-bench comparison line on the same prompt_version as every candidate — the QNT-128 baseline `f1aa25` was on a pre-QNT-133/QNT-136/QNT-137 prompt and re-using it would have anchored every candidate to the wrong target). Per-bench-alias `rpm` and `tpm` set at ~80% of each provider's published cap so the proxy queues over-cap requests instead of returning 429 — the eval sees latency, not failures. Harness: a single `--model` flag on `python -m agent.evals` plumbs the alias through a module-level `set_model_override()` in `agent.llm` so plan, synthesize, and the LLM-as-judge call all hit the model under test in one process; the alias suffix encodes into the run_id so per-model aggregates over `evals/history.csv` work without a schema column. Empirical: 8 sweeps × 16 records = 128 new history rows; Llama-4-Scout-17B wins every comparable measure (16/16 hallucination_ok, judge 4.94, cosine 0.342, p50 elapsed 836 ms) versus Qwen3-32B's 12/16 hallucination_ok and Llama-3.3-70B's 9-clean-records 0.336 cosine / 1 110 ms p50. Qwen confirmed unsafe (4 records with invented magnitudes for AAPL P/E, JPM EPS, UNH headlines, plus a Groq function-call collapse on META synthesize). Two candidates self-disqualified through the harness: gemma-3-27b-it has no JSON-mode support so every synthesize call 400'd into an empty thesis (cosine 0.000 across all 16 — the unambiguous tell that `hallucination_ok=16/16` was vacuous), and gemini-3.1-flash-lite-preview hit upstream 429s on 3/16 records despite a `rpm: 12` cap below the published 15. Recommendation landed in same PR: `equity-agent/fallback-qwen3` → `equity-agent/fallback-llama4scout` (same provider/key, 5× the default's TPD, strictly better than Qwen on every measure). Holding off on Scout-as-default — its lead over Llama on cosine is sub-noise (+0.006), the lead on judge is biased by self-judging, and Llama has months of production track record vs Scout's zero. Two bench-surfaced follow-ups filed in the doc but out of scope for the ship: (1) `traced_invoke` drops `model_name`/`usage_details` for `with_structured_output` responses since they aren't `AIMessage`, so reported "total tokens" is a lower bound on every Langfuse trace today, not just bench; (2) the prose references in `goldens/questions.yaml` underestimate cosine for the post-QNT-133 four-section thesis structure and should be re-shaped at next bench cycle. Calibration note for next time: I should have caught the reference TPD truncation before publishing the doc — the reference row's raw cosine 0.189 was 7/16 zeros from failed syntheses, not pure format drift; reviewer-ediff flagged this and the fix-commit's "0.336 (clean) / 0.189 (raw)" framing makes it audit-trail-visible. Output artifact: `docs/model-bench-2026-04.md` is the doc, structured so "why this model" is answerable in <30s; README link from QNT-66 captured in the doc's Follow-ups section.
- [x] `POST /api/v1/agent/chat` SSE endpoint for frontend chat page — QNT-56 **[shipped via QNT-74 in Phase 6]**
    - **Built after the CLI + evals** — same graph, different transport. The CLI shakes out prompt regressions before they reach the UI.
    - **Request**: `{"ticker": "NVDA", "message": "Analyze this stock"}` — stateless, single-analysis
    - **SSE events**: `tool_call` → `thinking` → `thesis` → `done`
- [x] Portfolio README — QNT-66 **[moved from Phase 7 — front-page recruiter artifact]**
    - Three portfolio screenshots deferred to QNT-139 (same milestone, same cycle) — README ships scaffold + capture recipe (`docs/screenshots/README.md`) and references QNT-139 inline so the deferral is visible.
- [x] Portfolio screenshots committed + recruiter-framing of README + docs/ sync — QNT-139
    - **Triggered by**: QNT-66's deferred screenshots loop. Captured `cli-thesis.png` / `langfuse-trace.png` / `dagster-lineage.svg` (Dagster's native SVG export, dimension-agnostic) under `docs/screenshots/`, plus `cli-thesis.txt` for repeatable regen. Original >=1600 px AC relaxed during sanity-check (cli-thesis 1341 px, langfuse-trace 1016 px) — shipping the README integration end-to-end matters more than re-shoot polish; resolution polish tracked under the ticket's Follow-ups.
    - **Bundled scope (folded into the same PR)**: README rewritten for recruiter-first reading — hero screenshot above the fold, broken `linear.app/noahwins/issue/QNT-XX` hyperlinks dropped (they 404 outside the workspace), dev workflow + ADR links retained. `docs/INDEX.md` backfilled with 6 missing retros + `design/` + `screenshots/` + `design-frontend-plan.md` + `model-bench-2026-04.md`. `docs/project-plan.md` cancelled QNT-82 line removed (sync-docs reconciliation).
    - Caught + fixed mid-smoke-test: `make setup` ran plain `uv sync` (skipped workspace members) → bumped to `uv sync --all-packages`. README's first-touch path is now end-to-end-verified.
    - Architecture diagram (mermaid, reused from `project-requirement.md` §3.1)
    - One Langfuse trace screenshot (a full `plan → gather → synthesize` run)
    - One Dagster lineage screenshot (the `ohlcv_raw → indicators → fundamental_summary` graph)
    - One agent-thesis screenshot (CLI output, NVDA or similar)
    - One-paragraph hallucination-resistance pitch (ties ADR-003 + QNT-67 eval harness)
    - This matters more than anything in Phase 7. Recruiters read the README before opening any code file.
- [ ] Verify: Run agent on 2-3 tickers, review thesis quality, confirm zero hallucinated calculations in Langfuse traces; hallucination eval passes on all golden-set questions; README renders correctly on GitHub with all screenshots

---

### Phase 6 — Frontend
**Scope**: Next.js terminal-style dashboard (TERMINAL/NINE design v2), ticker detail with TradingView charts, persistent agent chat panel. Deployed on Vercel.
**Dependencies**: Requires Phase 3. Requires Phase 5 for agent chat panel (depends on QNT-133 structured thesis output). News card gracefully degrades if Phase 4 not complete.
**Design reference**: `docs/design-frontend-plan.md` is the canonical UI spec — locks the three-pane layout (watchlist / detail / analyst), the elements each pane owns, the EOD framing (no live-tick chrome), and the field substitutions (EBITDA margin for op margin, ROE/ROA for ROIC, Quarterly/Annual/TTM tabs, no FWD).

- [x] Phase 6 frontend design assessment + canonical mocks — QNT-135 **[lands BEFORE any architecture or page work]**
    - **Triggered by**: Phase 5 → Phase 6 hand-off — design intent was scattered across `project-requirement.md`, ad-hoc mocks, and Slack-style notes; without a single canonical artifact every Phase 6 ticket would have re-litigated the layout. Shipped `docs/design-frontend-plan.md` as the locked spec (three-pane layout: watchlist / detail / analyst, EOD framing, field substitutions: EBITDA margin for op margin, ROE/ROA for ROIC, Quarterly/Annual/TTM tabs, no FWD) plus the canonical mock set. Every subsequent Phase 6 ticket references this doc by section instead of re-deriving the design; QNT-121's rendering-mode ADR, QNT-72's watchlist, QNT-73's detail page, and QNT-74's chat panel all consume it as input.
- [x] ADR-014: Next.js rendering mode per page (SSG / SSR / CSR) + cache strategy — QNT-121 **[written BEFORE any page code]**
    - **Slot drift fixed 2026-04-27 (Phase 5 retro)**: original ticket title was ADR-012, but ADR-012 went to QNT-136/137 (domain conventions in reports) and ADR-013 to QNT-104 (no Coolify); next free slot is ADR-014.
- [x] ADR-015: News-source + sentiment topology — QNT-140 **[written BEFORE QNT-72/73 consumer pages; gates QNT-131 / QNT-132]**
    - **Triggered by**: Phase 5 retro Phase 6 review — QNT-131 (`pending` sentiment state) and QNT-132 (subsystem provenance via `/health`) both carry "blocked on news-source decision" disclaimers. Decision is binary on two axes (news source: Finnhub vs AV `NEWS_SENTIMENT` vs current RSS; sentiment topology: async downstream asset vs in-line dep vs ships-with-news payload). Land before any consumer page so the provenance strip + news cards are wired against the real shape, not an assumed one.
    - Phase 4 retro carry-over: the Dagster quickstart-topology arc (QNT-100 → QNT-116, 17h across 4 incidents) and Qdrant point-ID arc (QNT-54 → QNT-120) both burned hours because the prod-vs-tutorial gap surfaced at deploy, not design. Next.js app-router has the same shape — SSR-by-default + RSC boundaries + SSE streaming don't all compose.
    - Each page/panel names: rendering mode, data-fetch location (RSC vs client vs API route), cache / revalidate strategy, and failure-mode rendering. Watchlist sidebar likely RSC + ISR; ticker detail middle pane SSR with client-side toggles; chat panel CSR + `fetch`/`ReadableStream`. **Also decides**: persistent right-pane chat (per design v2) vs `/chat` route — recommend persistent panel.
    - Output: `docs/decisions/014-nextjs-rendering-mode-per-page.md` — every subsequent Phase 6 ticket references it by section.
- [x] Initialize Next.js app in `frontend/` with Tailwind CSS — QNT-71
- [x] **Phase 6 backend support: indicators + fundamentals + sparkline + SPY** — QNT-134 **[gates QNT-72 + QNT-73]**
    - New indicators: SMA200, ADX(14), ATR(14), OBV, Bollinger %B, MACD bullish-cross flag.
    - Fundamental additions: EBITDA margin (substitutes op margin), gross/net margin bps deltas, TTM rollup asset (rolling-4Q sums of revenue / net income / FCF / EPS).
    - Sparkline field on `GET /api/v1/dashboard/summary` (60 daily closes per ticker — server-side, no N+1 client calls).
    - SPY benchmark ingest as a `BENCHMARK_TICKERS` constant — OHLCV-only, skipped from fundamentals/news/sentiment assets.
- [x] **News ingest migration: Yahoo RSS → Finnhub `/company-news`** — QNT-141 **[gates QNT-72 + QNT-73 news cards; per ADR-015]**
    - Schema migration 012 adds `publisher_name` + `image_url` + `sentiment_label` columns to `equity_raw.news_raw` (existing dedup key `(ticker, published_at, id)` unchanged).
    - Ingest rewrite: replace Yahoo RSS with Finnhub `/company-news` per partition; map `source` → ingest provenance, `publisher_name` → originating outlet, `image_url` → `image`, `sentiment_label` → `'pending'` (column exists per QNT-141 schema; QNT-131 classifier deferred — values stay `'pending'` indefinitely).
    - One-time 1y backfill on first run (Finnhub free tier explicitly allows 1y history per `freeTier: "1 year of historical news and new updates"` in the docs JSON).
    - `FINNHUB_API_KEY` added to `Settings` + `.env.example` + SOPS prod (per QNT-102).
    - Side-by-side density check (Finnhub vs RSS for one ticker, one week) before cutover; verify `news_embeddings` re-runs cleanly on Finnhub rows (point-id formula from QNT-120 is source-agnostic).
- [x] **Bound `news_embeddings` to recent + delta-only upsert — protect Qdrant free tier post-QNT-141** — QNT-142 **[durable fix for the QNT-141 backfill blast radius; calibration multiplier surfaced via `feedback_full_refresh_multiplier.md`]**
    - Switch `_FRESH_WINDOW_SQL` from `fetched_at >= now() - INTERVAL 7 DAY` to `published_at >= now() - INTERVAL 7 DAY` so the 28k-row Finnhub backfill (all `fetched_at = today`, 1y of `published_at` history) drops out of Qdrant scope. Pre-fix projection: 168k inferences/day, ~93x the calibrated free-tier budget.
    - Add delta-only upsert: scroll existing point IDs per ticker via `qdrant.scroll_ids` and skip rows whose `point_id(ticker, url_id)` is already indexed. Steady-state ticks now embed only new articles since the last tick.
    - Symmetric-window count check: both sides of `news_embeddings_vector_count_matches_source` scope to the same 7-day `published_at` window so neither the 1y CH backfill nor aged-out Qdrant points (no GC) lock the tolerance check into permanent WARN. Adversarial review (QNT-142) caught the Qdrant-side asymmetry that would have started false-failing within ~9 days otherwise.
    - Out of scope: Qdrant garbage-collection of points whose `published_at` aged past the window (the orphan check has the same monotonic-growth shape; needs a follow-up if `equity_news` storage starts tracking toward the 4 GB free-tier cap). News ingest schedule resume happens in Dagster UI post-deploy.
- [x] **Shift `news_raw_schedule` from 4h cadence to daily (02:00 ET)** — QNT-143 **[aligns ingest cadence with design v2 EOD framing; cleans the only sub-daily schedule in the project]**
    - `cron_schedule` flips from `0 */4 * * *` to `0 2 * * *` in `packages/dagster-pipelines/src/dagster_pipelines/schedules.py`. 7-day-per-week (no weekday filter) — news happens on weekends too (after-hours announcements, weekend macro, earnings warnings).
    - Aligns with `docs/design-frontend-plan.md` push-back #1 ("Drop the 'LIVE / 8ms / 5s refresh' framing") and the QNT-72 bottom-left status `EOD · 02:00 ET`. Post-QNT-142 the 4h cadence was functionally cheap (delta-only upsert), but cheap-and-aligned beats cheap-and-vestigial.
    - Inference token budget drops from ~270k/mo (5% of free tier) to ~70k/mo (1.4%); Dagster runs/day from 60 to 10; clean of overlap with ohlcv_daily (17:00 ET) and fundamentals_weekly (Sun 22:00 ET).
    - QNT-93 freshness check `_MAX_INGESTION_LAG_HOURS` bumped from 24h to 48h (2× cadence) — tighter would flap right before each next tick now that cadence equals the prior threshold.
- [x] **`dagster.yaml` env_vars drifted from Settings post-QNT-141 — bidirectional CI guard** — QNT-144 **[durable fix for the silent run-drop discovered while verifying QNT-143's first manual tick]**
    - Adds `FINNHUB_API_KEY` to `dagster.yaml` `run_launcher.config.env_vars` (QNT-141 added the key to `Settings` + `.env` + SOPS but skipped the DockerRunLauncher passthrough; per-run containers spawned by the cron started without the key and silently dropped at launch). Run history confirms 60 queued news_raw runs were dropped between 2026-04-27 16:00 UTC and discovery on 2026-04-29.
    - Strengthens `tests/dagster/test_run_launcher_env_vars.py` with the inverse-direction invariant: an explicit `ASSET_REQUIRED_SETTINGS` allowlist of Settings keys read inside DockerRunLauncher-spawned containers must be a subset of `env_vars`. QNT-125's existing forward direction (`env_vars ⊆ Settings`) caught QNT-59's drift but missed QNT-141's because it only checked one direction. Sweep clean today: `FINNHUB_API_KEY`, `QDRANT_URL`, `QDRANT_API_KEY`, `CLICKHOUSE_HOST`, `CLICKHOUSE_PORT`.
    - Unblocks QNT-143 AC-5 + AC-6 live verification on the next news_raw tick post-deploy.
- [x] **Qdrant GC: delete `news_embeddings` points whose `published_at` aged past the 7-day window** — QNT-145 **[converges Qdrant on ADR-009's literal rolling-7d definition; closes the QNT-142 out-of-scope advisory before it could trip the orphan check]**
    - Adds `QdrantResource.delete_points_by_filter(collection, query_filter)` with the same retry semantics as `upsert_points` (transient 5xx / 429 / transport errors retry up to `_MAX_RETRIES`; everything else fails loud).
    - At the tail of `news_embeddings`, derives a single Python-side `cutoff_ts = now - WINDOW_DAYS` and issues a ticker-scoped delete on `published_at < cutoff_ts`. GC runs unconditionally (even on quiet tickers with no fresh CH rows in the window) so a quiet feed cannot keep aged points alive.
    - `aged_ticker_filter(ticker, cutoff_ts)` (Range `lt`) and the QNT-142 count check's `_ticker_within_window_filter` (Range `gte`) form an exact partition at the same anchor: every Qdrant point is either kept or deleted, never both, never neither.
    - Tightens `news_embeddings_no_orphaned_vectors` (QNT-93) to scroll within the same 7-day window — without it the orphan check would silently become a no-op once GC ships, the same monotonic-growth asymmetry that broke QNT-142's count check before symmetrising.
    - `WINDOW_DAYS` promoted to a module-level constant in `news_embeddings.py` so the CH SQL window, GC filter, count check, and orphan check all reference one value (no risk of one drifting).
    - Pre-deploy verification (2026-04-29 per QNT-142's bucketed-distribution query): 5933 total points across 10 tickers, 923 in the 7-30d bucket. Post-deploy AC: every ticker has 0 points with `published_at < now() - INTERVAL 7 DAY` after the first scheduled tick.
- [x] **News pill: resolve true outlet beyond Finnhub redirect labels** — QNT-148 **[corrects ~78% of weekly news pills mislabelled "Yahoo"/"Benzinga"/"CNBC" by Finnhub redirect; ADR-016 commits the cross-ticker storage + dedup contract]**
    - **Triggered by**: QNT-73 ticker detail shipped with publisher pills reading whatever Finnhub put in `publisher_name` (often "Yahoo" / "Benzinga" / "CNBC" / generic feed-source labels) for `finnhub.io/api/news?id=...` redirect URLs — 4113 of the last week's 5135 news rows. The label can't tell us who actually wrote the article: "Yahoo" routinely covers republished Reuters / Bloomberg / Fool. QNT-73 partially patched the ~22% direct-outlet bucket via `domain(url)`; this ticket honestly resolves the redirect bucket at ingest.
    - Migration `024_alter_news_raw_add_resolved_host.sql` adds `resolved_host LowCardinality(String) DEFAULT ''` to `equity_raw.news_raw`. CD auto-applies on merge (per QNT-146).
    - `news_feeds.py` adds `resolve_publisher_host(url, client)` — HEAD with redirect-follow (5s deadline / 5 hops), GET fallback for 405/501 outlets that reject HEAD (`http.stream("GET", ...)` exits without `.read()` so the body is never fetched), short-circuit for non-finnhub.io hosts. Soft-fails to `''` on any error (timeout, non-2xx, redirect loop). `make_resolver_client()` factory centralises the timeout / redirect-budget config so the asset's pooled client and the resolver's own self-constructed client share one source of truth.
    - `news_raw` asset takes one pooled `httpx.Client` per partition (warm-pool ~30ms vs cold-connect ~150ms per article) and threads it through `_article_to_row`. ~210 calls per ticker per day in steady state, ~80% short-circuit on direct outlets — well under the 2-3 min per-partition budget.
    - `/api/v1/news/{ticker}` returns a single canonical `publisher` field via SQL `multiIf(resolved_host != '', resolved_host, domain(url) NOT IN ('finnhub.io', ''), replaceRegexpOne(domain(url), '^www\\.', ''), trim(publisher_name) != '', trim(publisher_name), '')`. Frontend `news-card.tsx` collapses to `item.publisher || "—"` with no further fallback.
    - **Intra-ticker dedup at API**: `WITH deduped AS (SELECT id, argMax(field, published_at) ... GROUP BY id)` — Finnhub returns same URL across ticks with drifted `datetime` epochs (3 NVDA rows for one article observed in pre-deploy data), and ReplacingMergeTree keys on `(ticker, published_at, id)` so timestamp drift can't be merged at storage. URL-hash equality on `id` is strictly stronger than `(host, headline)` and reuses the existing dedup column.
    - **Cross-ticker storage stays per-mention**: same URL under AAPL and MSFT lands as two rows by design (one per Finnhub `/company-news?symbol=X` mention). Cross-ticker dedup deferred to a future "Related across portfolio" surface — collapsing under one ticker would silently drop the article from the other ticker's feed.
    - **AC #10 (cross-ticker peer-news) deferred** with rationale in ADR-016 §4: Qdrant infra exists (`equity_news` collection + payload `ticker` filter), shipping it well needs a UI design pass against the recently-shipped watchlist + ticker-detail layouts, and marginal value is unclear without first observing how readers use the de-duplicated single-ticker feed shipped here. Revisit trigger: one week post-deploy usage data.
    - Pre-deploy data via tunnel: 7-day window had 5135 rows / 3360 distinct ids → fanout factor 1.528 (cross-ticker mention multiplier); ~78% finnhub.io redirects, ~22% direct outlets. Documented in ADR-016 §Context table.
    - **ADR-016**: cross-store identity table (Finnhub URL → news_raw composite key → Qdrant point ID), per-ticker attribution semantics (Finnhub matches on string-equality of `related[]`, not relevance), dedup contract (URL hash within ticker, no cross-ticker collapse), peer-news deferral. Extends ADR-015's table.
    - Adversarial review caught the GET-fallback `.read()` bug (would have downloaded full article bodies) before merge — refactored to `_fetch_head_or_get` helper that exits the stream context without consuming the body.
- [x] **Ticker detail: responsive labels at narrow viewports + period-aware P/E + simplified quote header** — QNT-153 **[third polish round on top of QNT-151/QNT-152; closed the 14" MacBook layout gap and surfaced one data-quality bug (QNT-154 filed)]**
    - **Triggered by**: After QNT-151 (text overflow) and QNT-152 (chart height + internal card scroll) shipped, the user reported the ticker detail page was still awkward at narrow viewports — TECHNICALS' `Daily/Weekly/Monthly` and FUNDAMENTALS' `Quarterly/Annual/TTM` tab labels overflowed the ~178 px card headers (TTM was being clipped off-screen on the FUND card), and the 5-stat quote-header `dl` was wrapping into 5 vertical rows because `flex-wrap + gap-x-6` was too generous for the ~560 px center pane.
    - **Original plan was abbreviation-only**: `D / W / M` for TECH tabs, `Q / A / T` for FUND tabs (two-span Tailwind pattern with `2xl:hidden` / `hidden 2xl:inline`), `Tech` / `Fund` for card titles below 2xl, `flex-nowrap + overflow-hidden` for the dl. Two-span pattern keeps the diff JS-free (just Tailwind class toggling) and the breakpoint at `2xl:` (1536 px) cleanly separates "external monitor" (full labels) from "MacBook 14"" (1512 px → abbreviated).
    - **Redirected mid-implementation by browser screenshots** (3 rounds):
        - Round 1 shipped, but `justify-center + overflow-hidden` clipped both edges of the dl at narrow widths (Open + Mkt cap + P/E TTM all cropped, only "0 - 210.30 VOL 223.74M" visible) and at 1920 px the 5 stats still overflowed → `flex-wrap` dropped P/E TTM to row 2 (awkward 4+1 split).
        - Round 2 fixed `justify-start` + tightened `gap-x-4` at 2xl+ + dropped Mkt cap/P/E below 2xl. Layout no longer broken but P/E still got clipped at 1920 px on borderline displays.
        - Round 3 (final): user redirected: the OHLCV strip below the chart already shows Open / Day range (= H/L) / Volume — header dl was a redundant duplicate. P/E is a valuation ratio that belongs in the Fundamentals card, not the quote header. Mkt cap is the same axis as the cap-tier chip, can ride alongside as the precise number. Quote header simplified to just identity (left) + price (right); `Stat` component deleted; `dl` removed entirely.
    - **Period-aware P/E in FUND card** — first row of the dl, label travels with the displayed period: `P/E` (Annual = price / annual EPS), `P/E annualised` (Quarterly = price / (Q EPS × 4) — single-quarter extrapolation, label flags it explicitly), `P/E TTM` (TTM = price / TTM EPS). Replaced the original "render `pe_ratio_ttm` regardless of tab" stub. New `peCell(row, price)` helper at the bottom of the file mirrors the existing `roeRoaCell` pattern but returns `{ label, value }` because the label varies, not just the value. Edge cases handled: null price → `—`, null EPS → `—`, denom (after × 4 if quarterly) === 0 → `—`, negative EPS → negative P/E (correct financial convention).
    - **Cap chip absorbs Mkt cap precision**: `<span>{capTier}<span className="ml-1 normal-case text-zinc-400">· {capValue}</span></span>` reads as `MEGA-CAP · 5.06T` — the `uppercase tracking-wider` outer chrome carries the chip identity while the inner `normal-case` span keeps the number readable.
    - **Surfaced QNT-154** (Backlog, no milestone): JPM quarterly + TTM rows ingest as zeros (revenue=0, net_income=0, ebitda=0) because yfinance returns banks' P&L in a Net-Interest-Income shape rather than the conventional Revenue/COGS/EBITDA tree. Annual is fine. Out of scope for this ticket (frontend renders what API returns); ADR-style triage decision (sector-aware empty-state vs bank-field mapping vs drop bank tickers) deferred to whoever picks up QNT-154.
    - **AC scope evolution noted on the Linear issue**: original AC #4 (`flex-nowrap dl`) and AC #5 (`Stat shortLabel + 'vs 30d' suffix`) were superseded by the redesign. AC #1-3 (tab/title abbreviations) shipped as planned. AC #6 (tsc clean) green. AC #7-9 (browser viewport tests at 1280/1920) verified by user across 3 review rounds.
- [x] **Price chart: 1M/3M/6M/YTD buttons zoom (not filter), older candles stay accessible** — QNT-155 **[surfaced 2026-05-01 during QNT-153 testing; TradingView-convention fix]**
    - **Triggered by**: clicking a range preset (1M / 3M / 6M / YTD) on `/ticker/{ticker}` was sliced data via `filterByRange` and passed the slice to `series.setData(...)`. Older candles were physically absent from the chart's internal dataset, so panning left hit a white void — broke the standard TradingView interaction model.
    - **Fix shape**: dataset and viewport are now decoupled. The full back-adjusted candles + volume + indicators reach `setData(...)` on every render; range presets resolve to a `[from, to]` window and call `chart.timeScale().setVisibleRange(...)` (or `fitContent()` for MAX) inside a separate effect. YTD anchors at `Date.UTC(year(latestBar), 0, 1)` per the issue spec. `filterByRange` deleted; replaced by `computeVisibleRange` which returns viewport bounds, not slices.
    - **Effect ordering**: data-push effect declared before viewport effect, both depend on `[candles]`; React commits them in declaration order so `setData` always runs before `setVisibleRange` within a single flush. Manual pan/pinch persists across renders because nothing in the viewport effect's deps changes when the user drags — only a preset click or a data refresh re-applies the viewport.
    - **SPY anchor tradeoff**: pre-fix SPY normalised against the first bar of the visible *slice* (re-anchored on range change — TradingView "Compare" convention). Post-fix anchors at full-extent first bar — simpler, line stable across zoom. If per-window 0% baseline is wanted back it's a separate ticket; comment in the file points there.
    - **Cross-ticker range persistence kept intentional** (AC #8): the `range` state lives on `PriceChart` and survives ticker swaps. A user comparing two stocks at the same window shouldn't have to re-click the preset on every watchlist hop. The viewport re-applies to the new dataset via the same range effect.
    - Adversarial reviewer (code-reviewer-ediff) returned SHIP with five non-blocking advisories; the only one I acted on was a misleading SPY-anchor comment I had written (claimed re-anchoring "defeats the comparison" — false; re-anchoring is the TradingView convention). Re-worded.
    - Author-verified all four [dev exec] AC in browser at /ticker/NVDA: 1M zoom + pan-left reveals older candles, MAX no refetch, ticker-switch preserves range, crosshair + indicator overlays render on older bars.
- [x] **Price chart: per-pane legends so sub-panes are self-identifying** — QNT-157 **[surfaced 2026-05-01 alongside QNT-155 testing; TradingView-convention follow-on]**
    - **Triggered by**: with the chart now showing up to 4 sub-panes (RSI / MACD / ATR / OBV) the panes were unlabeled — user had to remember which colored line was which, and 3 of the 4 indicators (RSI / ATR / OBV) had no value readout anywhere on the page (the header `indParts` block only covered SMA / BB / MACD). Standard TradingView convention is a small per-pane legend in the top-left that names the indicator and shows its current value at the hovered bar.
    - **Fix shape**: lightweight-charts ships no built-in legend (per the v5 docs page the issue links), so the implementation follows the documented HTML-overlay recipe — append a `<div>` to the chart container, position absolute, write `textContent` on every `subscribeCrosshairMove` tick. Pure formatters (`mainLegendText` / `rsiLegendText` / `macdLegendText` / `atrLegendText` / `obvLegendText`) live in their own `price-chart-legend.ts` module so they can be tested without spinning up a canvas. The chart-side effect owns DOM lifecycle: tear down on every dep change, build legends per current overlay set, re-pin Y offsets via `pane.getHeight()` summation, expose an updater closure through `updateLegendsRef` so the mount-time crosshair handler reads the latest per-bar lookup without re-subscribing.
    - **Anchoring tradeoff** (caught one-screenshot-into-impl-review): pane elements from `IPaneApi.getHTMLElement()` don't establish a positioning context (lightweight-charts lays panes out as `<tr>`/`<td>` children of an internal `<table>` — `position:absolute` on a child of an unstyled row falls through to the viewport, which is exactly what the first paint did — every legend stacked at the page top-left over the watchlist sidebar). Fix: anchor all legends to `containerRef` (the React-managed wrapper, which we explicitly set to `position: relative`) and compute pane Y offsets from `pane.getHeight()` accumulation. ResizeObserver on the container catches the responsive 300px↔400px breakpoint; the indicator-effect's `setStretchFactor` reflow is caught via the shared `overlays` dep on this effect.
    - **Design iteration during impl review**: shipped four UI revisions in the same impl session as the user eyeballed each candidate — (1) one-line crowded legend → (2) two-row wrap (ticker+SMA / BB+SPY) → (3) four-row split (ticker / SMA / BB / SPY) → (4) ticker + Vol dropped from the chart legend entirely (both already shown in the quote header above and the OHLCV crosshair readout, respectively). Final layout: main pane shows one row per toggled-on overlay group only; sub-panes (RSI / MACD / ATR / OBV) carry their own dedicated legend; OhlcReadout simplified from two rows to one (header now owns ticker + OHLCV + Vol + change; per-pane legends own SMA / BB / SPY / RSI / MACD / ATR / OBV).
    - **Test infra (new)**: added `npm test` script using Node 24's built-in `node --test` runner with native `.ts` support (type-stripping) and `node:test` + `node:assert/strict` — no vitest / jest / tsx dependency added. Tradeoff: ESM forces explicit `.ts` extensions on relative imports, so `tsconfig.json` gained `allowImportingTsExtensions: true` (safe pairing with the existing `noEmit: true`). 6 tests exercise every formatter (RSI / MACD / ATR / OBV / Main) plus the main-legend toggle matrix; satisfies AC #7 and gives future frontend tickets a zero-config path to add unit tests on pure modules.
    - **Adversarial-reviewer fixes**: code-reviewer-ediff agent flagged one BLOCKING (SPY%-anchor consistency between `spyOverlayLine` and the legend's % calc) and two ADVISORY (cleanup leak risk on parent-route unmount; `pane.getHeight()` returning stale 0 on the first frame after a `setStretchFactor` reflow). All three fixed: SPY anchor switched to `spyLine[0].value` so the legend tracks whatever convention `spyOverlayLine` uses; first `repositionAll()` wrapped in `requestAnimationFrame` so it reads post-layout heights; cleanup explicitly `el.remove()`s legend divs and nulls `updateLegendsRef.current` on unmount. (The "blocking" was technically equivalent in current shape because `backAdjusted` copies `adj_close` into `close`; the fix is defensive against a future change to either side.)
    - User-verified all four [dev exec] visual AC at /ticker/UNH and /ticker/NVDA: each sub-pane carries its labelled legend, main pane shows toggled-overlay groups only, crosshair-off falls back to most-recent bar, no candle overlap at any of 1/2/3/4 sub-pane stretch ratios.
- [x] **Price chart polish round (post-QNT-157 user-test bundle)** — QNT-158 **[surfaced 2026-05-01 during QNT-157 user-testing; one-PR bundle of five chart-correctness fixes]**
    - **Triggered by**: five small chart-correctness items the user flagged in quick succession after QNT-157 shipped — bundled into one ticket per the "don't split trivial follow-ups" pattern (every change touches the same `frontend/src/components/ticker/` files). Filed under QNT-158 mid-session once the change-set crossed the "trivial cosmetic" threshold; the user's "lump sum into one ticket" call confirmed the bundling.
    - **MACD histogram bars**: standard TradingView MACD pane is line + signal + histogram of `(macd - macd_signal)`. We were already surfacing the Hist value in the per-pane legend (added by QNT-157), but the bars themselves were missing — visually inconsistent. Adds a `HistogramSeries` on the MACD pane coloured green when the value is positive (bullish momentum) and red when negative; same pane index as the line + signal so they reflow together. New `MACD_HIST` key in the sub-pane teardown set so it gets cleaned up on overlay toggle.
    - **RSI 70 / 30 reference lines**: ADR-012 calls 70 / 30 the canonical RSI thresholds, but the pane had no visual cue for them — the user had to trust the right-axis scale. Adds two `series.createPriceLine(...)` calls on the RSI series itself (not the pane) — red dashed at 70, green dashed at 30, axis labels disabled per user preference. Lifecycle is automatic: lines disappear with the series when RSI is toggled off.
    - **Pane-drag legend tracking**: when the user dragged a divider between panes the heights changed but the container didn't, so the single `ResizeObserver` on the container missed the event and legends stayed at their pre-drag Y positions. Fix: in addition to the container, also `resizeObs.observe(p.getHTMLElement())` for every pane. ResizeObserver collapses duplicate notifications, so observing both surfaces is cheap and catches both the responsive 300px↔400px container breakpoint AND user-dragged pane reflows.
    - **Coloured legend (SMA / BB / SPY) + SPY label, no %**: the legend was monochrome zinc-300 — user couldn't visually link a number to its line on the chart. Each indicator name+value is now wrapped in a `<span style="color:..."> ` matching its series colour (SMA20 amber-400, SMA50 amber-500, SMA200 amber-600, BB↑/·/↓ all violet-400, SPY sky-400). The previous `SPY +44.33%` was anchored to the first loaded bar so the absolute % swung whenever the user changed the range preset (1M vs 5Y vs MAX) — misleading; replaced with a bare `SPY` label only (right-axis price label still carries the live value). Required switching `mainLegendText` → `mainLegendHtml` and the renderer from `textContent` to `innerHTML` so spans can be coloured; values all controlled (`toFixed` numbers + constant hex strings) so XSS surface is nil.
    - **Compact OBV right-axis**: OBV magnitudes commonly run into the billions (NVDA OBV ~5.9B); the default plain-number axis label showed "5918115640.22" which overran the right-axis gutter. Custom `priceFormat` via `fmtBigNum` (re-exported from `price-chart-legend.ts`) compacts to "5.9B" / "-2.1M" / etc. Other indicator panes keep default formatting (RSI 0-100, MACD small ±N, ATR small +N) — only OBV has the axis-overflow problem.
    - **Per-pane axis interaction (clarification, no code)**: user discovered that `wheel-only` doesn't scale a pane's price axis. Confirmed this is by-design: lightweight-charts' `mouseWheel` is bound to time-axis zoom; per-pane price scaling is via `axisPressedMouseMove.price` (click+drag on the right-axis), which is enabled by default. Same gesture model as the full TradingView app.
    - **Reviewer's wager paid**: QNT-157's adversarial-reviewer pre-empted exactly the pane-drag concern with an ADVISORY ("ResizeObserver fires when the container resizes but not when internal pane heights settle"). I ignored it on the QNT-157 ship because user-dragging a divider isn't a typical interaction — the user demonstrated otherwise within an hour. Confirmed pattern: reviewer-flagged advisories on UI ergonomics deserve a tighter triage even when not blocking.
    - User-verified all five [dev exec] visual AC at /ticker/NVDA: histogram bars render under MACD line/signal, 70/30 dashes appear on RSI pane with no axis labels, dragging a pane divider shifts every legend in real time, SMA/BB/SPY legend rows render in their respective line colours with no SPY %, OBV right-axis compacts to K/M/B notation.
- [x] **Watchlist polish: terminal title block + per-ticker logos + middle-pane logo + data: URL inlining** — QNT-162 **[surfaced 2026-05-02 cosmetic + perf round on the watchlist column]**
    - **Triggered by**: Lift the watchlist from "homework project" to "finance tool" framing without functional changes. Adds a Bloomberg-style stacked `Equity Data Agent` / `TERMINAL` title above the rail, Finnhub `/stock/profile2` logos per row with an initials-in-colored-circle fallback for missing logos (deterministic palette hashed on ticker, `<img onError>` swap), the same logo to the left of the big ticker in the quote header, drops the long company name from watchlist rows since the logo IS the recognition cue, and inlines the bytes as base64 `data:` URLs so the browser never round-trips to `static.finnhub.io` — all 10 watchlist logos + the quote-header logo arrive in one ~70-130 KB JSON response (gzipped: ~50 KB), amortised by a 24h Next Data Cache TTL. Backend caches in-process, pre-warms at FastAPI lifespan in a daemon thread. Code-reviewer-ediff caught two BLOCKING concurrency bugs pre-merge (lock held across HTTP I/O during cold-start; sync route inline-populated under that lock, starving uvicorn workers) — fixed by separating populate-only `_populate_lock` from lockless GIL-safe dict reads and bounding the request handler on a `threading.Event` set by the prewarm thread (30s ceiling, no Finnhub I/O on the request path); also caught SSRF via attacker-controlled logo URL, pinned to `https://static.finnhub.io` before the second GET. AC #6 ("no regression in scroll/click/selection") verified interactively during dev — user navigated AAPL → MSFT → TSLA and confirmed selection/hover/overflow scroll intact.
- [x] **Logos broken in prod: Finnhub now returns static2.finnhub.io, SSRF allowlist only permits static.finnhub.io** — QNT-163 **[surfaced 2026-05-02 from QNT-161 dev API logs spam — every ticker logged "host mismatch"]**
    - **Triggered by**: After QNT-161 shipped, dev API logs surfaced `finnhub logo URL host mismatch for <TICKER>: 'static2.finnhub.io'` for all 10 watchlist tickers + quote header. Finnhub had sharded their CDN; QNT-162's strict equality SSRF allowlist (`hostname == "static.finnhub.io"`) rejected every URL → cache stored `None` for every ticker → frontend served the colored-initials fallback instead of the real logo. Fix: replaced the constant with a `^static\d*\.finnhub\.io$` regex using `fullmatch`, accepting `static.finnhub.io`, `static2.finnhub.io`, and any future `staticN.finnhub.io` shard while still rejecting subdomain spoofs (`static.finnhub.io.evil.com`), suffix tricks (`static.finnhub.io2`), tld swaps (`static.finnhub.com`), non-numeric variants (`staticx.finnhub.io`), and non-https URLs. 13 new regression tests in `tests/api/test_logos.py` (4 accepted shards × parametrised, 9 rejected look-alikes × parametrised) lock the SSRF guard so a future "let's just allow ``*.finnhub.io``" sweep doesn't silently widen the surface. Pattern-not-example fix per `feedback_fix_pattern_not_example.md`: regex anticipates Finnhub's next shard rather than playing whack-a-mole on `static3`, `static4`, etc. **Follow-up in PR #181 (same ticket per "don't split trivial follow-ups")**: post-deploy verification surfaced JPM still returning null because its logo (83 KB) exceeded QNT-162's `_MAX_LOGO_BYTES = 64 * 1024` cap. The cap was set on a "real Finnhub PNGs come in around 5-15 KB" assumption that didn't hold for the larger-cap-icon brands (banks, healthcare). Bumped to 128 KB; added a regression test that pins the cap at >= 1.5× the largest observed legitimate logo (currently JPM at 83 KB) so a future tightening sweep can't silently re-break the same ticker class.
- [x] **Watchlist (left pane) + landing page** (`/`) — QNT-72
    - Persistent left-rail watchlist in `app/layout.tsx` (always visible, not a card grid).
    - 10 portfolio tickers + SPY benchmark; per-row: symbol · name · price · % change · 60-day inline-SVG sparkline.
    - Bottom-left status: `EOD · 02:00 ET` (sourced from QNT-132 `/health` provenance once available).
    - Calls `GET /api/v1/dashboard/summary` (single request returns all 11 tickers including sparkline arrays — no N+1).
- [x] **Ticker detail (middle pane)** (`/ticker/[symbol]`) — QNT-73
    - **Quote header**: price + change + OPEN / DAY RANGE / VOL / MKT CAP / P/E TTM (no FWD).
    - **Chart**: TradingView Lightweight Charts, candlestick + volume. **Renders `adj_close` as candlestick close** to avoid split discontinuities. Date-range toggle 1M / 3M / 6M / YTD / 1Y / 5Y / MAX (no 1D/5D — EOD only). Indicator overlay chips: SMA / BB / RSI / ATR / OBV (no VWAP). SPY benchmark overlay (default off).
    - **Technicals card**: Daily / Weekly / Monthly aggregation tabs; RSI / MACD / ADX / ATR / SMA 20/50/200 / Bollinger %B / OBV trend rows with signal-threshold decorations.
    - **Fundamentals card**: Quarterly / Annual / TTM tabs; Revenue (YoY %) / Gross margin (bps Δ) / EBITDA margin (bps Δ) / Net income / EPS / FCF / Cash·debt / ROE / ROA. ROE/ROA show `N/A · point-in-time` on Q tab, value · trailing 4Q on TTM tab.
    - **News card**: per-card publisher pill + date. **No sentiment chip in v1** — QNT-131 deferred 2026-04-28 (ADR-015 §"Revision history"). News card renders headline + publisher pill + date only.
    - **Provenance strip**: SOURCES + JOBS only — values read from `/health` (QNT-132), not hardcoded. SENTIMENT row dropped post-QNT-131-deferral (`provenance.sentiment` is `null`); AGENT row dropped as a constant not worth a contract for. Trimmed scope still demonstrates the data-driven UI pattern on the surface that actually changes (vendor swap, schedule shift).
- [x] **Analyst chat (right pane)** — QNT-74
    - Persistent right-rail panel that follows the active `/ticker/[symbol]` route (per ADR-014 §4 + Anti-pattern §6: chat is part of `app/layout.tsx`, not a `/chat` route — a route would tear down in-flight SSE on every ticker navigation).
    - `POST /api/v1/agent/chat` SSE endpoint streams `tool_call` / `tool_result` / `prose_chunk` / `thesis` / `done` events. **No Vercel AI SDK** — native `fetch` + `ReadableStream` + optional `eventsource-parser` (~2KB).
    - Tool-call rows show human labels + real args + result counts + latencies (e.g. `Reading price history · NVDA · 60d daily   60 bars · 38ms ✓`).
    - Inline data chips in agent prose (`+2.45% · close 4/24` style).
    - Structured thesis card per QNT-133: Setup / Bull Case / Bear Case / Verdict with stance + confidence bar. Asymmetric (empty bull or bear) renders gracefully.
    - Composer: `@<TICKER>` auto-set, tools-on toggle, cite-sources toggle. Source list in placeholder reads from `/health` (QNT-132).
- [x] **Agent chat SSE: production-hardening follow-up (disconnect cleanup, LLM timeout, error sanitization)** — QNT-150 **[QNT-74 adversarial-review punch list — six advisories that didn't block ship, bundled because they share an operational concern: the SSE path can hang or leak under failure modes the happy-path tests don't exercise]**
    - **Triggered by**: Adversarial review of QNT-74 surfaced six production-hardening advisories that did not block ship at the time. Filing them together because they share an operational concern: the chat SSE path can hang or leak resources under failure modes the happy-path tests don't exercise. (1) Client-disconnect cleanup — the `_stream` drain loop ran without a `try/finally`, so a browser disconnect mid-stream (FastAPI raises GeneratorExit) left `runner_task` running, burning LLM quota and a thread-pool slot until the graph naturally finished. Fix: wrap the entire drain + post-graph phase in `try/finally`; cancel `runner_task` and `await asyncio.shield(runner_task)` with `contextlib.suppress(CancelledError)` on every exit path. `asyncio.to_thread` can't actually kill the thread, but cancelling the asyncio task releases the queue + emitter so no further frames pile up after the SSE consumer is gone. (2) Top-level LLM timeout — `agent.llm.get_llm()` returned a `ChatOpenAI` with no `timeout`, so a hung LiteLLM proxy stalled the SSE connection forever. Fix: `LLM_REQUEST_TIMEOUT` (60s, free-tier-shaped) bounds every per-LLM call; `CHAT_RUN_TIMEOUT` (240s, ~4x per-call) bounds the whole run via a deadline check inside the drain loop and surfaces an explicit `agent-timeout` SSE error code. (3) Error sanitization — emitting `f"agent error: {type(exc).__name__}: {exc}"` to the client could leak LiteLLM auth-token strings, internal URLs, and stack context. Fix: stable user-facing constants (`_ERROR_DETAIL_AGENT_FAILED`, `_ERROR_DETAIL_AGENT_TIMEOUT`); raw exception detail only via `logger.exception`. Same treatment for the per-tool-failed event — graph-recorded `errors[name]` strings (which can carry `[error] http: 500 ... at http://api.internal:8000/...` from `agent.tools._format_error`) are now logged at WARNING and replaced with a stable `"<Tool> failed."` payload. (4) Stance-pill fallback — `STANCE_PILL[thesis.verdict_stance]` had no default; an unknown stance from a provider hallucination would render a pill with `undefined` className. Fix: `stancePillClass()` helper with `?? STANCE_PILL.mixed` so the runtime guard fires at the JSON.parse boundary even when a hallucinated stance slips past TS narrowing. (5) `done` event contract — module docstring listed `errors` in the payload but the emitted event omitted it (the TypeScript `DoneEvent` already did). Fix: docstring rewritten to match the actually-emitted shape (`{tools_count, citations_count, confidence}` plus `intent` once classify has run; pre-classify failures emit done without `intent`, matching the TS optional). (6) Streaming-race coverage — existing tests used a synchronous fake `invoke` that returned immediately, so the race between `runner_task.done()` and pending `call_soon_threadsafe` callbacks wasn't covered. Added `test_streaming_race_async_worker_drains_cleanly` (50ms tool delay) plus `test_client_disconnect_cancels_runner_task` (drives `_stream` directly, asserts the captured runner_task transitions to cancelled after `gen.aclose()`) and `test_run_timeout_emits_agent_timeout_error` (1s budget, asserts `agent-timeout` code + sanitized detail). Reviewer-agent (code-reviewer-ediff) caught two issues mid-pass: (a) the per-tool-failed error path bypassed the sanitization constants — fixed in the same PR with a `caplog`-based regression test; (b) the success-vs-failure `done` payload diverged silently on `intent` — fixed by clarifying the docstring contract so all three (TS type, docstring, emitted payload) agree on intent being optional.
- [x] **Agent chat SSE: emit intent event from classify_node so streaming label is correct from frame 0** — QNT-159 **[QNT-156 follow-up surfaced during user testing the same day; one-line user-visible bug, two-line root-cause]**
    - **Triggered by**: User testing QNT-156 noticed the chat panel's per-intent streaming label always read "streaming thesis…" regardless of which question they asked, even on comparison and conversational runs. Root-cause: `agent_chat.py:_stream` was only yielding the `intent` event AFTER `graph.invoke` returned (i.e., after every tool_call/tool_result event had already streamed), so `run.intent` was null in the panel for the entire tool-gathering phase and the label fell back to its default. The classify node runs FIRST in the LangGraph state machine and is fast (under 200 ms heuristic, under 800 ms with LLM) — its output was just being held back. The fix is option-1 from the QNT-159 ticket scope: add an optional `event_emitter: Callable[[str, dict], None] | None = None` kwarg to `build_graph`; the classify node calls it the moment intent resolves; the SSE wrapper provides an emitter that posts onto the existing asyncio queue via `loop.call_soon_threadsafe` (mirroring the QNT-74 tool-instrumentation pattern — same queue, same drain loop, same thread-safety primitive). Post-graph yield kept as an idempotent safety net for stubbed test graphs that bypass classify_node. CLI / eval harness pass no emitter; default None means zero behaviour change for those callers. Reviewer-flagged regression guard: the SSE-side ordering test is structurally tautological because the stubbed `_fake_build` calls the emitter itself, so a regression where production `classify_node` stops calling the emitter would leave the test green — added three direct unit tests on `build_graph` (pin emit-on-success, pin BLE001 swallow on a broken emitter, pin no-op behaviour when emitter is None) so the production code path is the thing under test. Verified end-to-end against live LiteLLM with one golden per intent: thesis judge=7, quick_fact judge=9, comparison judge=8, conversational judge=8, all hallucination + tool_call clean — additive change, evals path is untouched.
- [x] **Agent: extend intent set with comparison + conversational + domain-redirect fallback** — QNT-156 **[follow-on to QNT-149; closes the two visible gaps the two-intent baseline left]**
    - **Triggered by**: Two visible failure modes still in the chat panel after QNT-149 shipped — (1) open-ended asks like "Tell me about UNH" got the heavy four-section thesis even when the user wanted a quick read; (2) off-domain asks ("hi", "what can you do?", "what's the weather?") returned a thesis built from whatever reports the agent could fetch — noise, not a redirect, and zero guidance about what the agent CAN answer. QNT-156 extends the two-intent classifier to four shapes: adds `comparison` (multi-ticker side-by-side via a new `ComparisonAnswer` Pydantic schema with per-ticker sections + a qualitative differences paragraph; ADR-003 enforced — the LLM never computes a synthetic ratio or delta the reports don't already state) and `conversational` (greetings / capability asks / off-domain via a new `ConversationalAnswer` schema with short prose + an optional list of three example questions drawn from the agent's actual capabilities). Heuristic extended (multi-ticker + comparison phrase → comparison; greeting / capability / clearly-off-domain phrase → conversational; safety net: a conversational phrase paired with a covered ticker mention defers to the LLM, blocking false positives like "help me understand NVDA's RSI"); LLM classifier prompt + IntentDecision schema cover all four. Synthesize node now branches on intent and — critically — every failure surface (no reports gathered, structured-output crash, comparison parse failed) routes through a deterministic `domain_redirect` helper that builds a `ConversationalAnswer` listing covered tickers + biased suggestions, so the panel never sees a stack trace or blank state again. Conversational hard rule: NO digits in the answer body (the hallucination scorer treats one as a regression since there are no reports to cite); the helper guards `reason` at construction time so a future caller can't accidentally embed a status code or year. Two new SSE events (`comparison`, `conversational`) and two new chat-panel cards (`ComparisonCard` side-by-side; `ConversationalCard` with click-to-resubmit suggestion buttons); thesis + quick-fact cards stay hidden when their respective payloads are absent (same conditional pattern proven in QNT-149). Eval harness extended to render any of the four `to_markdown()` shapes; `prompt_version` hash widened to cover all four system prompts so a regression on any path is observable in `evals/history.csv`. Two new goldens (one comparison, one off-domain conversational) — live run against LiteLLM produced 2/2 hallucination_ok, 2/2 tool_call_ok, judge=8/10 each. Reviewer-agent caught two structural issues pre-merge: (1) `_hint_from_intent("quick_fact")` returned a label that didn't exist in the suggestion bank, silently degrading every quick-fact-failure redirect to the unbiased default — fixed to map to "technical" (where most quick facts live) with a regression test that asserts every intent's hint resolves to a real bank label; (2) `domain_redirect` accepted an unconstrained `reason: str` with no digit guard — fixed with a boundary assertion + regression test pinning the contract.
- [x] Adapt agent response shape to question intent (skip forced thesis on quick-fact prompts) — QNT-149
    - **Triggered by**: QNT-74 manual testing — the chat panel rendered the same heavy four-section thesis whether the user asked "should I be cautious about META?" or "what's the RSI right now?". The graph forced every input through `plan -> gather -> synthesize -> Thesis`; the user's `message` only nudged plan selection, not output shape. Net effect: rigid, ignores the question, every answer looks the same. The fix turns the plan-stage shape contract into a runtime decision: a `classify` node lands in front of `plan` (so the topology is now `classify -> plan -> gather -> synthesize`) and picks one of two response shapes per run. Two-layer classifier (cheap heuristic short-circuits the obvious cases; LLM-fallback via `with_structured_output(IntentDecision)` resolves the ambiguous middle), bias-to-thesis on every failure mode (LLM exception, malformed shape, observability-stack crash) so the QNT-67 hallucination + QNT-128 golden-set contracts cannot regress because of a misbehaving classifier. New `QuickFactAnswer` Pydantic schema (answer + cited_value + source) parallels `Thesis` and travels through the same structured-output path; the synthesize node branches on intent. Plan prompt bias also flips: thesis over-fetches, quick-fact narrows to the one report that answers the question (live: "What's the RSI right now?" → plan gathered `technical` only, skipping `fundamental` + `news`, finishing in 779 ms vs ~2 700 ms for thesis runs). API surface: two new SSE events — `intent` (one-shot routing preamble fired right after classify so the panel can pre-empt layout) and `quick_fact` (full QuickFactAnswer payload mirroring the existing `thesis` event); the thesis card hides when the run has no `thesis` payload, and a new compact `QuickFactCard` renders the short answer + monospaced cited-value chip when `quick_fact` arrives. Eval harness extended to render either `Thesis.to_markdown()` or `QuickFactAnswer.to_markdown()` so the hallucination/judge/cosine scorers see a real string regardless of shape; `prompt_version` hash widened to cover both system prompts so a regression on either path is observable in `evals/history.csv`. Live golden run (`20260501T162326Z-41e742`): 3/3 hallucination_ok, 3/3 tool_call_ok, judge=9 on the new `nvda-quickfact-rsi` golden vs judge=7 on the thesis goldens — clean signal that quick-fact-shaped questions get a tighter answer once they're routed to the right path. Reviewer-agent caught two structural issues that pre-merge: `classify_node` originally lacked the `BLE001` exception guard the other LLM-calling nodes wrap their calls in (a Langfuse SDK fault would have killed the run instead of biasing to thesis); and a bare `price` heuristic token would have silently mis-routed "price target" / "price action" questions to `quick_fact` against the bias-to-thesis contract — both fixed in the same PR with regression tests pinning the corrected behavior.
- [ ] **Backend support tickets that gate the design rendering**:
    - QNT-140 — ✓ **Done.** News-source + sentiment topology ADR (ADR-015) committed Finnhub `/company-news` + topology (a) async classifier asset.
    - QNT-141 — News ingest migration (Yahoo RSS → Finnhub `/company-news`); lands the schema columns (`publisher_name`, `image_url`, `sentiment_label`) that QNT-72/73 news cards consume. Per ADR-015.
    - QNT-131 — **Deferred 2026-04-28 to Backlog.** Classifier scoped to FinBERT (local CPU inference, no LLM coupling) when revisited. News cards in v1 ship without the `pend` chip. Trigger: UX evidence the chip is load-bearing, or thesis-eval signal that per-article sentiment moves the needle. See ADR-015 §"Revision history" (post-QNT-141, alongside QNT-142). The `sentiment_label` column from QNT-141 stays in place defaulted to `'pending'` for the future revisit.
    - QNT-132 — ✓ **Done 2026-04-28.** `/api/v1/health` provenance block (gates the bottom strip on QNT-73). **Trimmed 2026-04-28** to `sources` + `jobs.next_ingest_local` only. SENTIMENT/AGENT rows dropped — sentiment is `null` post-QNT-131 deferral; agent runtime is a constant. The remaining 2-line strip still carries the "no hardcoded UI strings" architecture signal on the values that actually flip (vendor swap, schedule shift). Suffix derived from the schedule's `execution_timezone` (single source of truth) — change the schedule, the strip flips.
    - QNT-133 — agent thesis output restructured to Setup / Bull Case / Bear Case / Verdict (gates the thesis card render in QNT-74) — *Phase 5 milestone*
- [ ] Generate TypeScript types from FastAPI's `/openapi.json` via `openapi-typescript` (`make types`) — do not handwrite types in `lib/api.ts`
- [x] **Public-chat abuse prevention: rate limit + per-IP cost cap + auth model + CORS lockdown** — QNT-161 **[gates QNT-75; landed before the public deploy because the Hetzner chat endpoint becomes an availability-bomb the moment the URL leaks]**
    - **Triggered by**: QNT-75 (Vercel deploy) makes `POST /api/v1/agent/chat` reachable to anyone who lands on the deployed frontend. Pre-QNT-161 the endpoint had no rate limit, no auth, no per-IP token budget, a permissive `*.vercel.app` CORS regex, and only the QNT-156 4000-char message cap as a guard. The threat model isn't a runaway monthly bill (Groq is free tier) — it's demo uptime. A scraper that exhausts Groq's daily TPD = the agent stops working = every subsequent recruiter sees a broken demo until quota reset = portfolio bounce. ADR-017 commits the project to **option (a) truly public** — no API key, no signup — because any auth wall destroys the one-click recruiter flow. Defense-in-depth instead: SlowAPI per-IP rate limit (`5/min, 30/hr, 100/day` with 429 + `Retry-After`), in-memory per-IP daily Groq token budget (~10K tokens/IP/day) + global TPD circuit breaker (~50% of model TPD) — both with UTC-midnight reset, both surfacing a deterministic conversational-redirect SSE event on exhaustion (no graph invocation, no LLM cost). Token tracking via a `contextvars.ContextVar`-bound `TokenUsageTracker` that the agent's `get_llm()` reads and attaches as a LangChain callback — propagates through `asyncio.to_thread` automatically. CORS allowlist tightened from open `*.vercel.app` regex to a project-pinned origin set + `CORS_ALLOWED_ORIGIN_REGEX` env (so leaked previews for unrelated Vercel projects can't drive traffic). Prompt-injection input filter (Pydantic field validator) rejects C0/C1 control chars and overlong-token (>500 char) runs. Frontend chat panel surfaces a small `demo: ~30 queries/hour per visitor; powered by Groq free tier` hint so the cap is set as expectation BEFORE 429s land; 429 path serves the SlowAPI JSON detail string verbatim. Sentry burst-alert hooks (`record_burst_alert` / `record_breaker_trip`) wired ahead of QNT-86 — minimal `sentry_sdk.init` guarded by `SENTRY_DSN`; without DSN, alerts log at WARNING. **FAIL CLOSED contract** locked by `tests/agent/test_litellm_fail_closed.py`: walks `litellm_config.yaml`, audits every chat-reachable alias + their fallback chain, asserts only Groq / Gemini / Google providers — a future contributor adding an Anthropic alias trips a CI failure. 33 new tests (27 in `test_security.py`, 5 in `test_litellm_fail_closed.py`, plus 4 patches to existing tests for the tightened CORS) covering: per-IP budget exhaustion → conversational redirect, global breaker trip → redirect + Sentry alert, redirect doesn't charge tokens (avoids billing-the-apology), 6th request in 60s returns 429 with friendly body, control-char rejection (NUL/BEL/ESC/DEL × 4), overlong-token rejection, newline/tab allowed, CORS regex allows pinned project + denies unrelated, burst alerter dedup + per-IP isolation, token tracker integration (post-run debit by observed spend), Sentry capture falls back to log when DSN unset. **Code-reviewer-ediff caught 3 BLOCKING issues pre-merge** — fixed in the same PR with regression tests: (1) `slowapi.util.get_remote_address` returned Caddy's container IP behind the reverse proxy (collapsed every visitor into one rate-limit + token-budget bucket), replaced with custom `client_ip()` reading `X-Forwarded-For` (leftmost) → `X-Real-IP` → `client.host`; (2) the LiteLLM config audit's permit-`groq/openai/` filter used `text.find("openai/")` once globally, suppressing any `anthropic/` violation when a single `groq/openai/` token existed elsewhere — switched to `re.finditer` with per-match position check; (3) `set_token_tracker` ran before the outer `try/finally`, so a `build_graph` crash mid-construction left a stale `contextvar` bound to the request task — moved inside the try with a `runner_task` None-guard in finally. End-to-end manual verification on real uvicorn: 100 reqs no XFF → 5 × 200 + 95 × 429; 10 reqs with distinct XFF → 10 × 200 (each treated as a separate IP, locking the per-IP fix).
- [x] **Deploy frontend to Vercel** — QNT-75 **[Phase 6 deploy ticket; ADR-018 commits the project to a free Cloudflare quick tunnel over a paid named-tunnel domain]**
    - **Triggered by**: QNT-71 (Next.js scaffold) and QNT-161 (public-chat abuse prevention) both Done — the gating prereqs cleared. Original Phase 6 plan assumed buy-a-domain + Caddy + Let's Encrypt for HTTPS termination on the FastAPI side, but for a portfolio piece the ~$10/yr ongoing cost and DNS-management overhead were cost-without-benefit. ADR-018 picks Cloudflare's free quick-tunnel mode instead: `cloudflared` runs on Hetzner with `--url http://api:8000`, makes an outbound connection to Cloudflare, and Cloudflare assigns a `*.trycloudflare.com` HTTPS hostname that the Vercel frontend reads via `NEXT_PUBLIC_API_URL`. Net effect: end-to-end HTTPS for $0, FastAPI port :8000 closed to the public internet (bound to `127.0.0.1` only), free Cloudflare WAF + DDoS protection sit in front of the API. The single trade-off: trycloudflare hostnames rotate on every cloudflared restart — with the image pinned to `2026.3.0` and `restart: unless-stopped`, rotation in practice happens 1-2× a year (kernel reboots, deliberate version bumps). Recovery runbook in `docs/guides/vercel-deploy.md` is ~5 min: read new URL from `docker logs cloudflared`, paste into Vercel env, redeploy. Caddy retained under a dormant `prod-caddy` profile so the named-tunnel + own-domain upgrade path stays one-command activatable. **Code-reviewer-ediff caught 3 BLOCKING issues pre-merge** — fixed in the same PR: (1) cloudflared image was `:latest` (floating tag would silently rotate the URL on every deploy + risk breaking-change pulls), pinned to `:2026.3.0` matching the litellm pin pattern in the same compose file; (2) `restart_if_changed "Caddyfile" caddy` in `deploy.yml` would silently no-op after the profile move (compose `--profile prod restart caddy` doesn't see services in `prod-caddy`), removed with a comment explaining the dormant-profile rationale; (3) `docker compose --profile prod up -d --build` was missing `--remove-orphans`, so the existing caddy container would have lingered indefinitely after the profile move — added the flag, eliminating the manual ssh cleanup step the deploy guide originally documented. CORS infra inherits from QNT-161 with no code change required (allowlist + project-pinned regex already in `packages/api/src/api/security.py`); the `.env.example` documents both `CORS_ALLOWED_ORIGINS` and `CORS_ALLOWED_ORIGIN_REGEX` for the prod env file. Reviewer's advisory items deferred as non-blocking: cloudflared in `docker compose ps` could surface false DOWN alerts (recovers fast in practice); `depends_on: service_healthy` semantics on container recreate are best-effort (acceptable in steady state); trycloudflare URL is in docker logs (acceptable, docker group is restricted on prod). Operational shape — the four ACs are all post-deploy verifications the user owns: Vercel project link, env var, CORS allowlist edit in `.env.sops`, browser-side dashboard check.
- [x] **Cross-cutting**: Ticker list is sourced from `GET /api/v1/tickers` across every page (watchlist, detail-page switcher, chat-panel selector) — never hardcoded. Inherits QNT-78's ⏳ PENDING AC; hardcoding the list anywhere defeats the endpoint's purpose. Phase 3 lesson. **Verified 2026-05-02 via prod walkthrough**: watchlist server-fetches from `/api/v1/tickers`; chat-panel selector pulls the same list; no hardcoded ticker arrays in `frontend/src/`.
- [x] Verify: Watchlist renders 10 tickers + SPY with sparklines; ticker detail shows quote header / chart / technicals / fundamentals / news / provenance strip with all data; chat panel streams tool calls in real time and renders structured thesis; provenance strip values flip when a config value changes (QNT-132 verification). **Verified 2026-05-02 in prod** via https://equity-data-agent-ynr2.vercel.app — all surfaces render with live data; CORS preflight returns the Vercel origin; SSE chat streams tool calls + structured thesis end-to-end.

---

### Phase 7 — Observability & Polish
**Scope**: Tracing, alerting, and production hardening.

- [x] Observability stack: Dozzle logs UI + Prometheus/Grafana/cAdvisor metrics — QNT-103 **[unified log/metrics dashboards on top of the per-IP rate-limit and breaker-trip alerting that QNT-161 already wired in Phase 6; QNT-86 (Sentry) lands first — see Phase 6 retro phase-review notes for the dependency ordering]**
    - **Triggered by**: Apr 19 2026 retro — unified logs UI + resource trend visibility are the observability surfaces missing from the current setup. Enables diagnosing slow leaks before they become outages.
- [x] Integrate Sentry for FastAPI error tracking (`sentry-sdk[fastapi]`, uses `SENTRY_DSN` from `.env`) — QNT-86
    - **Triggered by**: QNT-161 wired the abuse-control hook surfaces (`record_burst_alert` / `record_breaker_trip`) and a minimal `sentry_sdk.init` guarded by `SENTRY_DSN` ahead of this ticket, leaving release tagging, chat-stream error capture, and PII-scrub verification to land here. Phase 6 retro (2026-05-02) re-scoped the AC away from a greenfield init toward COMPLETING what QNT-161 stubbed: `release=settings.GIT_SHA`, `traces_sample_rate=0.1`, `auto_session_tracking=True`, `send_default_pii=False`. Chat SSE error paths from QNT-150 now forward via a new `sentry_capture_exception` helper — graph crashes are captured with the original exception object (preserving the worker-thread traceback that FastAPIIntegration's auto-capture middleware never sees), timeouts are captured as a synthetic `TimeoutError` so they cluster in the same Sentry issue keyed by ticker + budget. Added `/api/v1/_debug/sentry` verification endpoint gated on `settings.ENABLE_SENTRY_TEST` in prod (default off so a scraper looping on the path can't burn the 5K errors/mo free-tier quota; flip to 1 for one deploy when verifying, then revert). The `cors_aware_exception_handler` was extended to forward to Sentry explicitly because a registered `Exception` handler can short-circuit FastAPIIntegration's auto-capture on some sentry-sdk paths — Sentry dedups by stack-trace fingerprint so the redundant call is harmless. Promoted `GIT_SHA` from raw `os.environ.get(...)` to `Settings.GIT_SHA` and swept the existing `_health_payload` callsite for consistency. 12 new tests covering init kwarg shape (release / sample rate / session tracking / PII scrub), debug endpoint gating (404 in prod without override; raises in dev), `_sentry_capture_exception` no-op when DSN unset + DSN-set forwarding + SDK-failure swallow, and the chat SSE error → Sentry forwarding contract (graph crash → original exception forwarded; timeout → synthetic TimeoutError forwarded). Code-reviewer-ediff caught 2 blocking issues pre-merge fixed in the same PR: (1) raw `os.environ.get(...)` for `GIT_SHA` and `ENABLE_SENTRY_TEST` violated the "all config via shared.Settings" rule — promoted both to Settings fields and swept the existing `_health_payload` callsite; (2) a redundant test registered a permanent `/_test/cors_handler_forwards` route on the shared `main_module.app` instance, leaking into every later test — deleted the test since the same code path is covered by `test_debug_sentry_raises_in_dev`.
- [x] Add Dagster alerting on asset materialization failures — QNT-62
    - **Triggered by**: QNT-125 retro (2026-04-24 outage) — 200/200 Dagster runs failed at dequeue for ~20h while host/container alerts stayed silent (queue-drop is a runtime failure mode, not a container or host signal). QNT-125 added the CI guard against env-var drift; QNT-62 closes the sibling alert gap by wiring a `run_failure_sensor` to the existing `DISCORD_WEBHOOK_URL` (originally added by QNT-101 for `docker-events-notify`). Sensor fires once per terminal RUN_FAILURE — op-level `DEPLOY_WINDOW_RETRY` and run-level `DEPLOY_WINDOW_RUN_RETRY_TAGS` both transparently absorb intermediate retries before reaching the sensor. Dedup via `instance.get_run_records` with `(job_name, partition, status=FAILURE, updated_after=cutoff)` filter — current run's own FAILURE is included by Dagster's transition-before-dispatch ordering and excluded explicitly to avoid self-suppression. Dedup window 10 min, query `limit=100` for 10× burst headroom (worst case is one failure per ticker partition). Scope is `(job, partition)` not `(asset, partition)` — equivalent for this repo's jobs (each is single-asset or a tightly-coupled multi-asset group failing as one unit), and multi-asset jobs intentionally produce one alert per stuck partition rather than N. Discord message format mirrors `docker-events-notify.sh` (`[ASSET FAILURE] job=`x` asset=`y` partition=`z` step=`w`` header + fenced exception block + run URL) for visual consistency in the same channel. Settings additions: `DISCORD_WEBHOOK_URL` (shared with QNT-101) and `DAGSTER_BASE_URL` (default `http://localhost:3000` — prod webserver is private SSH-tunnel-only so localhost works for the operator clicking the run link). 11 unit tests cover message shape, dedup query (current-run-exclusion + only-current-run boundary), webhook helpers (truncation / HTTP error / non-2xx swallowing), and sensor integration against `DagsterInstance.ephemeral()`. Code-reviewer-ediff initially returned `FIX FIRST` flagging (1) (asset, partition) vs (job, partition) scope drift, (2) `limit=10` pagination edge if >10 failures land in one window, and (3) dedup test bypassing the real query path via monkeypatch — addressed in the same PR by documenting the (job, partition) scope as intentional, bumping limit to 100 with explicit headroom rationale, and documenting the two-layer test strategy (helper unit-tested directly with stub instance, sensor end-to-end against ephemeral instance for the no-dedup branch).
- [x] Implement retry logic for flaky external API calls (yfinance, news APIs) — QNT-63
    - **Triggered by**: Phase 7 audit found three remaining gaps after the Phase 6 retry surfaces (ClickHouse OperationalError loop, Qdrant transient handler, asset-level RetryPolicy) had landed: (1) yfinance 429 path string-matched the exception message and ignored Retry-After, (2) `fetch_company_news` was single-shot HTTP — a transient 5xx burned a full asset retry slot when the next attempt would have succeeded, (3) `Backoff.EXPONENTIAL` had no jitter on the three external-fetch assets. New `retry_helpers.py` parses Retry-After as either delta-seconds or HTTP-date (RFC 9110 §10.2.3), clamped to a 300s ceiling so a hostile / mis-formatted header can't sleep the asset for hours, and walks `exc.response.headers` defensively so any library that attaches the original 429 response (httpx, requests) flows through the same path — yfinance's `YFRateLimitError` discards the response so the existing fixed-delay fallback still applies there. `fetch_company_news` gains an intra-attempt retry loop (max 2 retries within one asset op = 3 total tries) that retries 429 + 5xx, honors Retry-After when present, falls back to 1s/2s exponential otherwise, and bubbles 4xx auth/bad-symbol errors immediately so the asset doesn't burn budget on permanent failures. All three ingest assets (`ohlcv_raw`, `fundamentals`, `news_raw`) switched from `Backoff.EXPONENTIAL` to `Backoff.EXPONENTIAL + Jitter.PLUS_MINUS` matching `DEPLOY_WINDOW_RETRY`'s pattern. 29 new tests: 18 in `test_retry_helpers.py` cover the parser (delta-seconds, HTTP-date, past-date clamp-to-zero, ceiling clamp, garbage, bytes input, case-insensitive headers, plain-dict fallback, missing-response fallback, plain non-HTTP exception); 11 in `test_external_fetch_retries.py` cover the asset call sites (yfinance 429 with Retry-After sleeps the header value before re-raising, yfinance 429 without header re-raises immediately for the asset RetryPolicy to handle, non-429 failures skip silently per pre-existing behavior, Finnhub single 5xx survives without burning asset retry, three consecutive 5xx bubble to caller, 4xx does not retry, 429 Retry-After drives sleep duration in fetcher, 5xx without Retry-After uses exponential base delay) plus a structural pin on the jittered RetryPolicy on all three assets. Code-reviewer-ediff returned SHIP with 5 advisory items (test hygiene around `_make_handler_sequence` bounds + `_disable_finnhub_intra_sleep` autouse footprint; minor RFC divergence on `Retry-After: 0` falling to base delay; per-sleep cap × retry count interaction; pre-existing fundamentals quarterly/annual blocks still swallow 429 — explicitly out of scope per ticket). Out of scope per ticket: ClickHouse and Qdrant retries (already covered in Phase 6).
- [x] Load test FastAPI endpoints (confirm response times under 10 tickers) — QNT-65
    - **Triggered by**: Phase 6 surfaced (and Phase 7 retro confirmed) that demo uptime depends on the QNT-161 abuse-prevention path (rate-limit / per-IP token cap / global Groq TPD breaker / LiteLLM fail-closed), not on endpoint CPU/IO. Two re-scopes followed: (1) Phase 6 swap from generic latency probe to demo-protection load test, then (2) Phase 7 swap from real-TPD-burn to lowered-cap behavioral re-trip. Re-scope #2 (2026-05-06) audited `tests/api/test_security.py` (751 lines) and found every demo-protection AC already covered by unit tests — re-tripping the same code path under load shape adds no signal. Final scope shrunk to a one-off p50/p95/p99 baseline for the 5 read endpoints × 10 tickers loop. Implementation: `scripts/load_test_baseline.py` (asyncio + httpx driver, semaphore concurrency, linear-interp quantiles, latency-on-200-only with err column as canary, >5% error rate exit-non-zero) staged via `scp` to Hetzner `/tmp` then `docker cp`'d into the api container — eliminates SSH + Caddy hops, leaving FastAPI + ClickHouse query latency only. Run record at SHA `6e9ad60` on Hetzner CX41 (2026-05-06): 500 reqs / 0 errors / 14.1 s; quote p50 597 / p95 1112 ms (slowest, stitches OHLCV + 30-bar avg vol + TTM P/E); ohlcv 408/703; indicators 496/711; fundamentals 495/915; news 390/817. None hit p99 < 500 ms — acceptable for current ISR + Vercel deploy-hook architecture (most page loads serve build-time-pinned responses). Demo-protection (`tests/api/test_security.py`) and TokenBudget/SlowAPI race-condition probes documented as out-of-scope with rationale. Code-reviewer-ediff returned SHIP with 5 advisory items addressed pre-merge: tightened "no network hop" to "eliminates SSH + Caddy hops" (loopback round-trip still exists), comment on TICKERS divergence from `shared.tickers` + meta-JSON drift detection, clarified warm-up scope (50 reqs, not 500), added latency-on-200 interpretation note, explicit /tmp transient SCP disclaimer per `feedback_prod_hotfix_scp.md`.
- [x] Write integration tests for critical paths (ingestion → calculation → report → agent) — QNT-64
    - **Triggered by**: closes the QNT-148 mock-only coverage gap that surfaced as a prod 500 (`ClickHouse ILLEGAL_AGGREGATION` on a CTE alias collision) — mock-only API tests can't catch CTE alias collisions / `ILLEGAL_AGGREGATION` / GROUP BY scope errors by design. Phase 6 retro (2026-05-02) added an explicit AC: at least one integration test per API router (logos, quote, ohlcv, fundamentals, news, search, agent_chat) that exercises the real ClickHouse client against a fixture-loaded test DB, not a mock. Implementation covers all four critical paths in dedicated test files: Path 1 (ingestion → CH) verifies `ClickHouseResource.insert_df` round-trips for OHLCV / fundamentals / news + `ReplacingMergeTree` idempotency on replay; Path 2 (raw → indicators) runs `compute_indicators` against the committed AAPL fixture, persists, and asserts the snapshot matches expected values at `rtol=1e-6` plus warm-up `Nullable` round-trip; Path 3 (FastAPI) exercises every router via `TestClient` + real `api.clickhouse.get_client` against fixture-loaded data covering reports (×4), data (ohlcv/quote/fundamentals/indicators/news/dashboard), tickers, logos no-key fallback, search Qdrant fallback, health, and agent_chat unknown-ticker SSE — plus a concurrent-query smoke test pinning the `autogenerate_session_id=False` fix; Path 4 (agent → tools → API) monkeypatches `agent.tools.httpx.get` to forward through `TestClient`, covering all `default_report_tools` plus `search_news` end-to-end. Conftest applies `migrations/*.sql` idempotently on session start (so CI runs without a separate `make migrate` step), refuses to run if `equity_raw.{ohlcv_raw,news_raw,fundamentals}` has rows (tunneled-prod safety gate, bypassed by `CI=true`), auto-truncates between tests, and stubs `_fetch_logo_data_url` session-wide so daemon prewarm threads can't race the logos test's `FINNHUB_API_KEY=""` monkeypatch. 35 new integration tests + 2 existing pass in 11.7 s (well under the 5 min AC); full suite (587 tests) passes in 22.2 s. Code-reviewer-ediff caught 2 advisory issues fixed pre-merge: (1) prod-data safety gate originally only scanned `ohlcv_raw`, missing the "fresh prod with news/fundamentals but no OHLCV" bypass — extended to scan all three; (2) `api.qdrant.get_client` `lru_cache` wasn't cleared between integration tests, letting a polluted client from one search test leak into a sibling test's `QDRANT_URL` monkeypatch — added cache-clear to both Path 3 and Path 4 autouse fixtures.
- [ ] Verify: End-to-end run on all 10 tickers, review Langfuse dashboard, confirm no orphaned errors in Sentry

---

### Post v1 Improvements
**Scope**: Targeted upgrades to the v1 surfaces (agent grounding, report quality) once the core stack ships and feedback starts to land. Each item is incremental — no new phases, no new infra.

- [x] Enrich news report + add company knowledge tool — QNT-175
    - **Triggered by**: two adjacent gaps surfaced once the v1 agent went live: (1) the news report template still rendered the Phase-3 "sentiment scoring pending Phase 4" placeholder even though Phase 4 had populated `sentiment_label` + `body` on every `news_raw` row for months — the data was in ClickHouse, never plumbed into the agent-facing report; (2) `TICKER_METADATA` only carried `name`/`sector`/`industry`, leaving the synthesize step with no static context about what each company does, who it competes with, or which metrics matter — so qualitative thesis claims had to lean on LLM prior knowledge (the QNT-67 hallucination scorer's exact failure mode). Implementation bundled both: the news template now selects `sentiment_label` + `substring(body, 1, 120)` from `news_raw FINAL`, prints a body snippet + `Sentiment: <label>` line under each headline, and computes a real distribution (`N bullish / N neutral / N bearish across N scored of N headlines -- net bullish/bearish/balanced`) in plain Python; legacy Yahoo-RSS rows whose `sentiment_label='pending'` render as `Sentiment: N/A` and the SIGNAL line names the un-scored state explicitly so a single Finnhub article doesn't read as net-bullish off a feed of un-scored ones. The new `packages/api/src/api/templates/company.py` is a static report builder (no DB query — keeps the tool available when the warehouse is unreachable) backed by extended `TICKER_METADATA` carrying `description` / `key_competitors` / `key_risks` / `watch` for all 10 tickers; `GET /api/v1/reports/company/{ticker}` exposes it. The agent picks it up as a fourth tool: `default_report_tools()` registers `company`, `REPORT_TOOLS = (company, technical, fundamental, news)`, `_TOOL_LABELS` adds "Reviewing company" so the SSE chat panel renders the tool row, and `_parse_plan` enforces the plan-prompt bias as code (thesis + comparison force-include `company` even if the plan LLM omits it; quick_fact strips it — a single-metric ask doesn't benefit from a description / competitor list). The Pydantic source Literals in `agent.thesis` / `agent.quick_fact` / `agent.comparison` were extended to match `REPORT_TOOLS` so the system prompt's `(source: company)` citation actually validates at structured-output parse time — code-reviewer-ediff caught the schema/prompt drift pre-merge (Pydantic would have null-coerced or rejected company citations, silently dropping grounding evidence). 9 new tests pin the surfaces: news sentiment + body-snippet rendering, pending-as-N/A, company report shape + 10-ticker coverage (so a future TICKER_METADATA edit can't strand a ticker on `(none recorded)`), `_parse_plan` force-include / strip behavior across all three intents, and a schema-Literal vs REPORT_TOOLS guard so the next tool-add can't drift the structured-output boundary again.
    - **Post-merge follow-ups** (4 PRs over the same evening, all responding to issues surfaced by the first prod thesis runs against the new 4-tool plan — none big enough for their own ticket but each targeting a measurable thesis-quality regression):
        - **PR #240 — chat trace stitching + token-budget bump.** Two unrelated cleanups bundled: (a) `agent_chat._runner` was missing the `@observe(name="agent-chat")` wrapper that the CLI had on `agent.__main__.analyze`, so each graph node's `@observe` decorator opened its own root trace in Langfuse. The chat path's classify / plan / gather / synthesize spans appeared as four separate top-level traces, while the CLI ran under one parent. One-line wrapper restored topology parity. (b) `CHAT_TOKENS_PER_IP_PER_DAY` 10K → 30K and `CHAT_TOKENS_GLOBAL_PER_DAY` 50K → 200K. Old global cap was 50% of just the primary's 100K TPD, sized before the LiteLLM auto-fallback to Llama-4-Scout (500K TPD) existed; effective ceiling is 600K TPD combined and 50K left ~550K of permanent unused headroom. New per-IP cap supports ~7-10 thesis runs/visitor; new global cap is ~33% of combined ceiling, reserves ~150K for daily ingest + own eval sweeps. Also bumped `mako` 1.3.10 → 1.3.12 in the same commit because pip-audit started failing CI on every branch (CVE-2026-44307; transitive dep, unrelated to QNT-175).
        - **PR #241 — thesis fetches every tool deterministically.** Prod sweep showed plan-LLM noise: same prompt, "Give me a thesis on NVDA" → all 4 tools, "Give me a thesis on AAPL" on the next request → only [company, fundamental, news] (technical dropped). The plan-prompt bias text already said "include every report that is even marginally relevant" but the LLM was narrowing under its own steam. Fix: skip the plan-LLM call entirely for `thesis` + `comparison` — `plan = list(available)` is deterministic. Quick_fact KEEPS the plan-LLM call because narrowing to the single relevant report is the whole point of that path. Drops ~250 tokens per thesis run (no plan call) and removes a real source of variance the QNT-175 force-include for `company` only half-fixed. 5-ticker post-deploy sweep confirmed 4/4 tools fired on every run.
        - **PR #242 — bullets cite underlying metrics, not the SIGNAL line + decimal-preservation.** Two prompt-layer tightenings driven by direct screenshot review of the first NVDA thesis: (a) bull/bear bullets were citing each report's `## SIGNAL` aggregate verdict ("the technical report indicates a BULLISH signal with 2/3 indicators agreeing") instead of the underlying metrics that drove the verdict (RSI 60.8, MACD posture, P/E vs threshold, revenue YoY %). The SIGNAL line is meta-summary the stance field already conveys; bullets exist to show their work. New rule + counter-example added to both bull and bear sections of `SYSTEM_PROMPT`, naming SIGNAL-line bullets as "non-bullets". (b) The first NVDA thesis rendered `SMA-50 (187.72)` from the technical report as `SMA-50 level of 18772` in the verdict action — a $187.72 support level became a fictitious $18,772 target by stripping the decimal. Added byte-for-byte format-preservation rule to the `verdict_action` description, careful to stay digit-free in the prompt body (the QNT-136 regression guard `test_system_prompt_contains_no_multi_digit_literals` would have failed on a `187.72` example). Same NVDA prompt post-fix produced underlying-metric bullets (`Revenue +73.21% YoY (source: fundamental)`, `P/E of 42.06, indicating a rich valuation (source: fundamental)`) and `SMA-50 (187.72)` correctly preserved.
        - **PR #243 — encourage news citation when headlines bear on the question.** Post-#242 prod sweep: only 2 of 5 thesis runs cited news (NVDA, TSLA); AAPL / META / JPM omitted news entirely despite each having 10 fresh on-topic headlines. Root cause: the model was cherry-picking fundamental's numerical metrics (easy to cite verbatim) and treating news as decoration. Without an explicit rule, "no number to cite verbatim" became "skip the source". Fix: a dedicated paragraph in the bull/bear sections naming news's role (catalyst evidence: partnerships, analyst notes, regulatory actions, product launches, demand signals, recalls, guidance, lawsuits) and saying the thesis "should cite at least one in either bull or bear" when news has on-topic content. The only valid opt-out is "no news headline materially bears on the question", not "fundamental has more numbers to cite". Kept rule 1 (no padding) precedence so off-topic news can still be skipped. Same 5-ticker post-deploy sweep: 5/5 cited news, AAPL/META/JPM all surfaced real catalysts (MP-Materials/Pentagon deal, multibillion-dollar tax benefit, Ripple/Mastercard tokenized Treasury settlement); TSLA correctly split news across bull (Optimus production) and bear (camera recall) — exactly the bull-or-bear allowance the rule was written for.
    - **Pattern lesson**: this five-PR fan-out matches `feedback_backend_pack_consumer_loop` — adding a backend surface (4th tool + new endpoint) needs the first downstream consumer to actually exercise it before the rough edges show up. Three of the four follow-ups (#241 plan-LLM noise, #242 bullets-cite-SIGNAL, #243 no-news-citation) were prompt-layer regressions only visible once a real thesis ran against the new 4-tool plan; none were detectable from unit tests. PR #240 was infra noise (trace topology + token-budget sizing) that surfaced from the post-deploy verification, not the thesis content. Next time a tool is added to the plan, plan to ship the consumer fixes in the same cycle.

- [x] Focused-analysis intents + composer cleanup — QNT-176
    - **Triggered by**: two adjacent gaps in the chat shape after the v1 4-tool plan landed. (1) The classifier had four intents (`thesis` / `quick_fact` / `comparison` / `conversational`) but no clean path for "give me a technical analysis of NVDA" or "what's the news sentiment on META?" — those questions either defaulted to `thesis` (over-fetched every report and produced a four-section card the user did not ask for) or got clipped to `quick_fact` (one cited value, too short for a real read). (2) The composer in `chat-panel.tsx` exposed `tools` and `cite` toggle buttons that had become footguns: `tools_enabled` was wired but every useful chat answer needs tools, and `cite_sources` had been a no-op on the backend since QNT-149. Hiding the synthesis contract (ADR-003: every numeric claim cites a source) behind a toggle taught the wrong mental model. Both gaps were small individually but both touched the same composer + intent surface, so bundled.
    - **Implementation**: Three new intents (`fundamental` / `technical` / `news_sentiment`) added to `agent.intent.Intent`. Plan node narrows DETERMINISTICALLY to `[company, <matching_report>]` — no plan-LLM call (the user named the domain explicitly; nothing to disambiguate). Heuristic triggers explicit phrases ("fundamental analysis", "technicals", "TA on", "chart setup", "news sentiment", "what is/'s the sentiment", "news read") with a mutual-exclusivity guard: if the user names multiple focuses in one breath ("triangulate technicals AND fundamentals"), the focused branch defers to thesis. New shared Pydantic class `FocusedAnalysis` with `summary` / `key_points` / `cited_values` and a `focus` discriminator — one class covers all three shapes, synthesize node re-asserts the focus value defensively against an LLM that emits the wrong literal. New SSE event `focused`; new `FocusedAnalysisCard` React component with a coloured pill per focus (sky / emerald / amber). Composer cleanup dropped `tools_enabled` / `cite_sources` from `ChatRequest`, the React composer JSX, and the SSE request body; backend unconditionally calls `default_report_tools()`. Schema-pin test (`test_chat_request_rejects_legacy_toggle_fields`) blocks silent re-introduction.
    - **Eval coverage**: three new golden records (`jpm-fundamental-deepdive`, `v-technical-analysis`, `unh-news-sentiment`) bring the suite to 22 records; new `test_every_intent_has_at_least_one_golden` regression guard iterates `get_args(Intent)` so a future intent addition lands with a golden alongside it (or fails the test loudly). Backfilled `expected_intent: quick_fact` on the previously unpinned `nvda-quickfact-rsi` record. `_prompt_version` now hashes `FOCUSED_SYSTEM_PROMPT` so prompt edits to the focused path are visible in `history.csv` as a different version.
    - **In-flight scope cut**: section C (markdown rendering via `react-markdown` + `remark-gfm`) was rolled back during sanity-check. The LLM does not currently emit markdown markers in its prose — structured Pydantic fields (`bull_case`, `key_points`, etc.) already carry list-shaped content, and the system prompts do not ask for `**bold**` / `inline code` / `- bullet` emphasis. Adding a renderer for markup that never appears violated CLAUDE.md ("don't add features beyond what the task requires"). Both deps were uninstalled and `ProseBlock` reverted to plain spans. If a future prompt-tuning ticket adds markdown emphasis to the synthesis prompts, re-add the renderer alongside that change.
    - **Review finding**: `code-reviewer-ediff` flagged the `"sentiment on"` and `"headlines on"` heuristic tokens as over-broad — phrases like "market sentiment on the sector" or "headlines on the bond market" would fire `news_sentiment` with no ticker present. Tightened to drop both; remaining tokens all carry "news" or "sentiment" in unambiguous focused-ask context. Regression test added.

- [x] Chat empty-state suggested questions + header cleanup — QNT-178
    - **Triggered by**: post-QNT-176 visual review of the empty chat panel — the page burnt vertical space on a single placeholder line ("Ask a question to start a research session. Active ticker: TSLA.") with zero discoverability for the answer shapes the agent supports. The QNT-176 focused-analysis intents (fundamental / technical / news_sentiment) shipped invisible; a visitor had to know to type "technical analysis of NVDA" to find them. Two adjacent footguns alongside it: the active ticker was repeated three times (header pill + empty-state body + composer @ticker chip), and the header carried decorative `LangGraph` / `Cited` pills that read as internal jargon for a portfolio site without telling the user anything actionable.
    - **Implementation**: new `EmptyState` component renders 4 ticker-parametrised suggestion buttons (one per main intent shape: thesis + the three QNT-176 focused intents). Click prefills the composer textarea + focuses, doesn't auto-send — gives the user a chance to edit before pressing Send (distinct from `ConversationalCard` suggestions which auto-send mid-conversation; same visual via a shared `SuggestionButton` component, different parent click contracts). Composer was lifted to a controlled component (`value` / `onChange` / `focusKey` props) so external clicks can prefill. `focusKey: number` is bumped by the parent and watched via `useEffect` to call `textareaRef.current.focus()`; skips the `focusKey === 0` initial render so opening the page doesn't steal focus from the rest of the layout. Header pills (`LangGraph`, `Cited`) dropped — the subheader trust line ("demo: ~30 queries/hour per visitor · powered by Groq free tier") is the canonical advertisement and was already there. Redundant "Active ticker: TSLA" body line removed; the @ticker chip in the composer carries it.
    - **Review finding**: `code-reviewer-ediff` flagged a cross-ticker leak — clicking "Technical analysis of TSLA", then navigating to /ticker/AAPL without sending, would leave "TSLA" in the composer with @AAPL chip; one accidental Send away from running a TSLA question against the AAPL run path. Pre-lift, `Composer` owned its own state but the same scenario existed (the panel stays mounted across route changes); the prefill button just makes hitting it much easier. Fixed via React's "adjust state during render" pattern (https://react.dev/learn/you-might-not-need-an-effect#adjusting-some-state-when-a-prop-changes): track `prevTicker` in state, conditionally `setComposerInput("")` during render when it shifts. Avoided the setState-in-useEffect anti-pattern (ESLint flagged on the first attempt) and avoided lifting the input even further into a `key={ticker}` wrapper.
