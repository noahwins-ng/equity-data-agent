[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "langgraph>=1.2.9",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    # Required by langfuse.langchain.CallbackHandler (QNT-181). The Langfuse
    # SDK's LangChain integration imports `langchain` directly; core+openai
    # alone aren't enough.
    "langchain>=0.3.0",
    "langfuse>=4.14.0",
    "httpx>=0.28.1",
    # QNT-209: SqliteSaver for cross-request session memory (chat followup).
    "langgraph-checkpoint-sqlite>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent"]

[tool.uv.sources]
shared = { workspace = true }
