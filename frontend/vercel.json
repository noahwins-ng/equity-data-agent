{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"production\" ] && exit 1; git diff --quiet HEAD~1 HEAD -- . 2>/dev/null || exit 1"
}
